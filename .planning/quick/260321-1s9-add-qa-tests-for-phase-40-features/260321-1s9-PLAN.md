---
phase: quick
plan: 260321-1s9
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/qa_tests
autonomous: true
requirements: [QA-COVERAGE]

must_haves:
  truths:
    - "QA tests exercise the profile subcommand group (clone, set, get, validate, delete, rename, enable, disable, status)"
    - "QA tests exercise the --no-travel-opt slice flag"
    - "QA tests exercise the --quiet and --json global flags on commands added in phase 40"
  artifacts:
    - path: "scripts/qa_tests"
      provides: "New profile and travel-opt test groups"
      contains: "group_profile_manage"
  key_links:
    - from: "scripts/qa_tests"
      to: "slicecore profile"
      via: "CLI invocation in group_profile_manage"
      pattern: "profile clone|profile set|profile enable"
---

<objective>
Add QA test coverage for features implemented in phases 40-43 that are missing from scripts/qa_tests.

Purpose: The qa_tests script has not been updated since phases 40-43 added: (1) global --quiet/--color flags, (2) --json on convert-profile/import-profiles, (3) --no-travel-opt on slice, (4) the entire `profile` subcommand group (clone/set/get/reset/validate/delete/rename), and (5) profile enable/disable/status commands. The existing `group_profile` only tests legacy top-level profile commands.

Output: Updated scripts/qa_tests with new test groups covering all gaps.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@scripts/qa_tests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add travel-opt and global-flags test groups</name>
  <files>scripts/qa_tests</files>
  <action>
Add two new test groups to scripts/qa_tests, inserted after the existing `group_slice` function:

1. **group_travel_opt** (new group "travel-opt"):
   - `run_test "slice with --no-travel-opt" "$SLICECORE" slice "$TMPDIR_QA/cube.stl" --no-travel-opt -o "$TMPDIR_QA/no_travel.gcode"` - verifies the flag is accepted
   - `run_test_file_exists "no-travel-opt output exists" "$TMPDIR_QA/no_travel.gcode"` - output was produced
   - `run_test "slice default (travel opt enabled)" "$SLICECORE" slice "$TMPDIR_QA/cube.stl" -o "$TMPDIR_QA/with_travel.gcode"` - baseline comparison succeeds

2. **group_global_flags** (new group "global-flags"):
   - `run_test "slice --quiet suppresses progress" "$SLICECORE" slice "$TMPDIR_QA/cube.stl" --quiet -o "$TMPDIR_QA/quiet_out.gcode"` - --quiet flag works on slice
   - `run_test "analyze --quiet" "$SLICECORE" analyze "$TMPDIR_QA/cube.stl" --quiet` - --quiet on analyze
   - `run_test "slice --color never" "$SLICECORE" slice "$TMPDIR_QA/cube.stl" --color never -o "$TMPDIR_QA/nocolor.gcode"` - --color flag accepted
   - `run_test_json "convert-profile --json" "$SLICECORE" convert-profile "$TMPDIR_QA/test_profile.json" --json -o "$TMPDIR_QA/converted_json.toml"` - JSON output from convert-profile (requires the test_profile.json fixture from group_profile; generate it inline if group_profile hasn't run)

Update ALL_GROUPS to include "travel-opt" and "global-flags".

Update `needs_fixtures` loop to include "travel-opt" and "global-flags".

Add `group_travel_opt` and `group_global_flags` calls in `main()` after `group_slice`.

For group_global_flags, create the test_profile.json fixture inline at the start of the function (same content as in group_profile) since group ordering may vary.
  </action>
  <verify>
    <automated>bash scripts/qa_tests --group travel-opt,global-flags --no-color 2>&1 | tail -20</automated>
  </verify>
  <done>Both new groups pass all tests. --no-travel-opt, --quiet, --color, and --json flags are exercised.</done>
</task>

<task type="auto">
  <name>Task 2: Add profile-manage and profile-activate test groups</name>
  <files>scripts/qa_tests</files>
  <action>
Add two new test groups to scripts/qa_tests, inserted after the existing `group_profile` function:

1. **group_profile_manage** (new group "profile-manage"):
   This group tests the `profile` subcommand group from phases 42-43. It needs a profiles directory fixture. Create the fixture setup at the top of the function:
   - Create `$TMPDIR_QA/profiles/` directory with machine/ filament/ process/ subdirectories
   - Create a minimal machine profile TOML at `$TMPDIR_QA/profiles/machine/test-printer.toml` with content: `[metadata]\nname = "test-printer"\ntype = "machine"\n\n[settings]\nnozzle_diameter = [0.4]\n`
   - Create a minimal process profile TOML at `$TMPDIR_QA/profiles/process/test-process.toml` with content: `[metadata]\nname = "test-process"\ntype = "process"\n\n[settings]\nlayer_height = 0.2\n`

   Tests (all use `--profiles-dir "$TMPDIR_QA/profiles"`):
   - `run_test "profile clone" "$SLICECORE" profile clone test-printer --name my-printer --profiles-dir "$TMPDIR_QA/profiles"` - clone creates a user profile
   - `run_test_file_exists "cloned profile exists" "$TMPDIR_QA/profiles/user/my-printer.toml"` - verify file was created (note: clone writes to user/ subdir)
   - `run_test "profile validate" "$SLICECORE" profile validate my-printer --profiles-dir "$TMPDIR_QA/profiles"` - validate the cloned profile
   - `run_test "profile set" "$SLICECORE" profile set my-printer layer_height 0.3 --profiles-dir "$TMPDIR_QA/profiles"` - set a value
   - `run_test_output_contains "profile get" "0.3" "$SLICECORE" profile get my-printer layer_height --profiles-dir "$TMPDIR_QA/profiles"` - get returns the set value
   - `run_test "profile rename" "$SLICECORE" profile rename my-printer my-printer-renamed --profiles-dir "$TMPDIR_QA/profiles"` - rename works
   - `run_test "profile delete" "$SLICECORE" profile delete my-printer-renamed --yes --profiles-dir "$TMPDIR_QA/profiles"` - delete with --yes
   - `run_test_expect_fail "profile clone invalid name" "$SLICECORE" profile clone test-printer --name "-bad-name" --profiles-dir "$TMPDIR_QA/profiles"` - name validation rejects leading hyphen
   - `run_test_expect_fail "profile delete nonexistent" "$SLICECORE" profile delete nonexistent-profile --yes --profiles-dir "$TMPDIR_QA/profiles"` - error on missing profile

   IMPORTANT: Check the actual CLI flags by running `slicecore profile clone --help`, `slicecore profile set --help`, etc. to ensure the correct flag names. If `--profiles-dir` is not the correct flag, use whatever flag the commands accept (it may be inherited from the parent or be a positional). Adjust accordingly. The key thing is each command needs to point at the test fixture profiles directory.

2. **group_profile_activate** (new group "profile-activate"):
   Tests the enable/disable/status commands from phase 43:
   - `run_test "profile enable" "$SLICECORE" profile enable test-printer --profiles-dir "$TMPDIR_QA/profiles"` - enable a profile
   - `run_test_json "profile status --json" "$SLICECORE" profile status --json --profiles-dir "$TMPDIR_QA/profiles"` - status returns JSON
   - `run_test_output_contains "profile status shows enabled" "test-printer" "$SLICECORE" profile status --profiles-dir "$TMPDIR_QA/profiles"` - status includes the enabled profile
   - `run_test "profile disable" "$SLICECORE" profile disable test-printer --profiles-dir "$TMPDIR_QA/profiles"` - disable works
   - `run_test "profile list --all" "$SLICECORE" profile list --all --profiles-dir "$TMPDIR_QA/profiles"` - list with activation filtering
   - `run_test_expect_fail "profile enable nonexistent" "$SLICECORE" profile enable nonexistent-profile --profiles-dir "$TMPDIR_QA/profiles"` - error on missing profile

Update ALL_GROUPS to include "profile-manage" and "profile-activate".

Update `needs_fixtures` loop to include "profile-manage" (it needs the binary but not mesh fixtures; however the binary needs to be available so it should be in the needs_fixtures check, OR just ensure_binary is called before these groups which it already is).

Add `group_profile_manage` and `group_profile_activate` calls in `main()` after `group_profile`.

IMPORTANT: Before writing the tests, run `slicecore profile clone --help` and `slicecore profile enable --help` to verify the exact flag names and required arguments. Adapt the test commands to match the actual CLI interface.
  </action>
  <verify>
    <automated>bash scripts/qa_tests --group profile-manage,profile-activate --no-color 2>&1 | tail -30</automated>
  </verify>
  <done>Profile management and activation test groups pass. Clone/set/get/validate/rename/delete/enable/disable/status are all exercised with both success and failure cases.</done>
</task>

</tasks>

<verification>
Run the full QA test suite (skipping build and bench for speed):
```bash
bash scripts/qa_tests --skip build,bench --no-color
```
All existing tests still pass, and new groups (travel-opt, global-flags, profile-manage, profile-activate) also pass.
</verification>

<success_criteria>
- scripts/qa_tests has 4 new test groups covering phases 40-43 features
- --no-travel-opt flag on slice command is tested
- --quiet and --color global flags are tested
- --json on convert-profile is tested
- profile clone/set/get/validate/rename/delete are tested (phase 42)
- profile enable/disable/status are tested (phase 43)
- All new tests pass when run individually via --group
- Existing tests are not broken
</success_criteria>

<output>
After completion, create `.planning/quick/260321-1s9-add-qa-tests-for-phase-40-features/260321-1s9-SUMMARY.md`
</output>
