---
phase: 35-configschema-system-with-setting-metadata-and-json-schema-generation
plan: 05
type: execute
wave: 3
depends_on: ["35-02", "35-03"]
files_modified:
  - crates/slicecore-engine/src/support/config.rs
  - crates/slicecore-engine/src/seam.rs
  - crates/slicecore-engine/src/infill/mod.rs
  - crates/slicecore-engine/src/ironing.rs
  - crates/slicecore-engine/src/flow_control.rs
  - crates/slicecore-engine/src/custom_gcode.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "All support config structs (SupportConfig, BridgeConfig, TreeSupportConfig) derive SettingSchema"
    - "All support config enums (SupportType, SupportPattern, InterfacePattern, TreeBranchStyle, TaperMethod, QualityPreset, ConflictResolution) derive SettingSchema"
    - "Cross-module enums (InfillPattern, SeamPosition, GcodeDialect, IroningConfig fields) derive SettingSchema"
    - "All ~54 support/config.rs fields have #[setting()] annotations"
  artifacts:
    - path: "crates/slicecore-engine/src/support/config.rs"
      provides: "Support config structs/enums with SettingSchema derives"
      contains: "#[derive(SettingSchema)]"
    - path: "crates/slicecore-engine/src/infill/mod.rs"
      provides: "InfillPattern enum with SettingSchema derive"
      contains: "#[derive(SettingSchema)]"
    - path: "crates/slicecore-engine/src/seam.rs"
      provides: "SeamPosition enum with SettingSchema derive"
      contains: "#[derive(SettingSchema)]"
  key_links:
    - from: "crates/slicecore-engine/src/support/config.rs"
      to: "crates/slicecore-config-schema"
      via: "derive macro"
      pattern: "slicecore_config_derive::SettingSchema"
---

<objective>
Annotate all remaining config types outside config.rs: support/config.rs, cross-module enums, ironing config, custom gcode types.

Purpose: Complete the annotation coverage to reach 100% of config fields. These files contain ~54 support fields and several enums used as config field types.

Output: All config-related types across the engine crate annotated with SettingSchema.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-CONTEXT.md
@.planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-RESEARCH.md
@designDocs/TIER_MAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate support/config.rs structs and enums</name>
  <files>
    crates/slicecore-engine/src/support/config.rs
  </files>
  <read_first>
    crates/slicecore-engine/src/support/config.rs (full file -- all support config types),
    designDocs/TIER_MAP.md (tier assignments for support fields),
    crates/slicecore-config-schema/src/types.rs (SettingCategory::Support)
  </read_first>
  <action>
1. Add imports to support/config.rs:
```rust
use slicecore_config_derive::SettingSchema;
```

2. Annotate ALL enums in support/config.rs with `#[derive(SettingSchema)]`:
   - SupportType (4 variants: Normal, Tree, Organic, Hybrid) -- each gets `#[setting(display = "...", description = "...")]`
   - SupportPattern (5 variants) -- display names and descriptions
   - InterfacePattern (3 variants)
   - TreeBranchStyle (3 variants)
   - TaperMethod (~3 variants)
   - QualityPreset (~3 variants)
   - ConflictResolution (~3 variants)

3. Annotate SupportConfig struct:
   - `#[derive(SettingSchema)]`
   - `#[setting(category = "Support")]`
   - Key fields:
     - `enable`: tier=1, description="Enable support structures", affects nearly all support sub-settings
     - `support_type`: tier=2, description="Type of support structure", depends_on="support.enable"
     - `overhang_angle`: tier=2, description="Minimum overhang angle for support generation", units="deg", min=0.0, max=90.0
     - Most other support fields: tier=3, depends_on="support.enable"
   - `#[setting(flatten)]` on bridge and tree_support sub-struct fields

4. Annotate BridgeConfig struct:
   - `#[derive(SettingSchema)]`
   - `#[setting(category = "Support")]`
   - All fields tier=3 with appropriate descriptions, units, ranges

5. Annotate TreeSupportConfig struct:
   - `#[derive(SettingSchema)]`
   - `#[setting(category = "Support")]`
   - All fields tier=3, depends_on="support.support_type == Tree"
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo check -p slicecore-engine 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - support/config.rs contains `use slicecore_config_derive::SettingSchema`
    - support/config.rs contains `#[derive(SettingSchema)]` on SupportConfig
    - support/config.rs contains `#[derive(SettingSchema)]` on BridgeConfig
    - support/config.rs contains `#[derive(SettingSchema)]` on TreeSupportConfig
    - support/config.rs contains `#[derive(SettingSchema)]` on SupportType enum
    - support/config.rs contains at least 40 occurrences of `#[setting(`
    - `cargo check -p slicecore-engine` succeeds
  </acceptance_criteria>
  <done>All support config types fully annotated with SettingSchema derives</done>
</task>

<task type="auto">
  <name>Task 2: Annotate cross-module enums and config types</name>
  <files>
    crates/slicecore-engine/src/seam.rs,
    crates/slicecore-engine/src/infill/mod.rs,
    crates/slicecore-engine/src/ironing.rs,
    crates/slicecore-engine/src/flow_control.rs,
    crates/slicecore-engine/src/custom_gcode.rs
  </files>
  <read_first>
    crates/slicecore-engine/src/seam.rs (SeamPosition enum),
    crates/slicecore-engine/src/infill/mod.rs (InfillPattern enum),
    crates/slicecore-engine/src/ironing.rs (IroningConfig struct),
    crates/slicecore-engine/src/flow_control.rs (PerFeatureFlow struct/type),
    crates/slicecore-engine/src/custom_gcode.rs (CustomGcodeHooks struct)
  </read_first>
  <action>
For each file, add `use slicecore_config_derive::SettingSchema;` and annotate:

1. **seam.rs** - SeamPosition enum:
   - Add `#[derive(SettingSchema)]` to derive list
   - Add `#[setting(display = "...", description = "...")]` to each variant
   - Variants: Aligned, Random, Rear, NearestPoint, etc.

2. **infill/mod.rs** - InfillPattern enum:
   - Add `#[derive(SettingSchema)]` to derive list
   - Add `#[setting(display = "...", description = "...")]` to each variant
   - Variants: Rectilinear, Grid, Honeycomb, Gyroid, AdaptiveCubic, Cubic, Lightning, Monotonic, etc.
   - Each variant description should briefly explain the pattern's characteristics

3. **ironing.rs** - IroningConfig struct:
   - Add `#[derive(SettingSchema)]`
   - `#[setting(category = "Quality")]`
   - Fields: enabled (tier=2), speed, flow_rate, spacing, angle, etc.

4. **flow_control.rs** - PerFeatureFlow (if it's a struct with fields used in config):
   - Add `#[derive(SettingSchema)]` if it has annotatable fields
   - If it's just a type alias or simple wrapper, use `#[setting(skip)]` on the config.rs field that references it

5. **custom_gcode.rs** - CustomGcodeHooks struct:
   - Add `#[derive(SettingSchema)]`
   - `#[setting(category = "Advanced")]`
   - Fields for various G-code injection points

Note: If GcodeDialect is in slicecore-gcode-io (a separate crate that doesn't depend on slicecore-config-schema), do NOT add SettingSchema derive to it. Instead, handle it as a String type in the config field's setting annotation. The config field `gcode_dialect: GcodeDialect` will be treated as `ValueType::String` by the type inference in the derive macro.

After all files are annotated, verify the entire workspace compiles:
```bash
cargo check --workspace
```
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo check --workspace 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - seam.rs contains `#[derive(SettingSchema)]` on SeamPosition
    - infill/mod.rs contains `#[derive(SettingSchema)]` on InfillPattern
    - ironing.rs contains `#[derive(SettingSchema)]` on IroningConfig
    - `cargo check --workspace` succeeds with no errors
  </acceptance_criteria>
  <done>All cross-module config enums and structs annotated, full workspace compiles</done>
</task>

</tasks>

<verification>
- `cargo check --workspace` compiles cleanly
- All config types in the engine crate have SettingSchema derives
- Combined with plan 04, all ~387 fields are annotated
</verification>

<success_criteria>
- support/config.rs: 3 structs + 7 enums annotated
- Cross-module: SeamPosition, InfillPattern, IroningConfig annotated
- Full workspace compiles with all annotations
</success_criteria>

<output>
After completion, create `.planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-05-SUMMARY.md`
</output>
