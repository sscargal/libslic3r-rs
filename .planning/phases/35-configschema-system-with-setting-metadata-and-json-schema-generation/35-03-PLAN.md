---
phase: 35-configschema-system-with-setting-metadata-and-json-schema-generation
plan: 03
type: execute
wave: 2
depends_on: ["35-02"]
files_modified:
  - designDocs/TIER_MAP.md
autonomous: false

must_haves:
  truths:
    - "TIER_MAP.md lists all ~387 fields grouped by category with tier assignment and rationale"
    - "User has reviewed and approved tier assignments before annotation begins"
  artifacts:
    - path: "designDocs/TIER_MAP.md"
      provides: "Complete tier assignment map for all config fields"
      contains: "| Field |"
  key_links: []
---

<objective>
Produce the TIER_MAP.md design document mapping every config field to a progressive disclosure tier.

Purpose: This is a gate artifact -- the user must review tier assignments before the annotation plans execute. Uses OrcaSlicer's Simple/Advanced/Expert tab placement as baseline.

Output: `designDocs/TIER_MAP.md` with all ~387 fields categorized, tiered, and rationalized.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-CONTEXT.md
@.planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-RESEARCH.md
@crates/slicecore-engine/src/config.rs
@crates/slicecore-engine/src/support/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TIER_MAP.md with all fields tiered and rationalized</name>
  <files>
    designDocs/TIER_MAP.md
  </files>
  <read_first>
    crates/slicecore-engine/src/config.rs (all config structs and fields -- read fully),
    crates/slicecore-engine/src/support/config.rs (support config fields),
    .planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-CONTEXT.md (tier system definition)
  </read_first>
  <action>
Create `designDocs/TIER_MAP.md` containing:

1. **Header section** explaining the tier system:
   - Tier 0 (AI Auto): Empty for now -- populated in AI integration phase
   - Tier 1 (Simple): ~15 most essential settings a beginner needs (layer_height, infill_density, support_enable, etc.)
   - Tier 2 (Intermediate): ~60 settings for users who want more control
   - Tier 3 (Advanced): ~200 settings for power users
   - Tier 4 (Developer): Everything else (debug, calibration, niche)

2. **Methodology note**: OrcaSlicer baseline mapping:
   - OrcaSlicer Simple tab -> Tier 1
   - OrcaSlicer Advanced tab -> Tier 2
   - OrcaSlicer Expert tab -> Tier 3
   - Hidden/Debug/Niche -> Tier 4

3. **Tables grouped by category** (one table per SettingCategory). Each table has columns:
   | Field Key | Display Name | Tier | OrcaSlicer Tab | Rationale |

   For example, the Speed category table would include entries like:
   | speed.perimeter | Perimeter Speed | 1 | Simple | Core speed setting visible to all users |
   | speed.infill | Infill Speed | 1 | Simple | Primary infill speed |
   | speed.support | Support Speed | 2 | Advanced | Support tuning is intermediate |

4. **Summary counts** at the bottom:
   - Tier 1: N fields (target ~15)
   - Tier 2: N fields (target ~60)
   - Tier 3: N fields (target ~200)
   - Tier 4: N fields (rest)
   - Total: ~387

Go through EVERY field in config.rs and support/config.rs. Group by their natural SettingCategory based on which sub-struct they belong to:
- PrintConfig top-level fields -> Quality or General
- SpeedConfig -> Speed
- CoolingConfig -> Cooling
- RetractionConfig -> Retraction
- MachineConfig -> Machine
- AccelerationConfig -> Acceleration
- FilamentPropsConfig -> Filament
- LineWidthConfig -> LineWidth
- FuzzySkinConfig -> Advanced
- BrimSkirtConfig -> Adhesion
- InputShapingConfig -> Advanced
- ToolChangeRetractionConfig -> Retraction
- DimensionalCompensationConfig -> Advanced
- PostProcessConfig -> PostProcess
- TimelapseConfig -> Timelapse
- ScarfJointConfig -> Quality
- ToolConfig -> Machine
- MultiMaterialConfig -> MultiMaterial
- SequentialConfig -> Advanced
- PaCalibrationConfig -> Calibration
- SupportConfig -> Support
- BridgeConfig -> Support
- TreeSupportConfig -> Support

Use 3D printing domain knowledge to assign tiers. Key Tier 1 candidates:
- layer_height, first_layer_height, infill_density, infill_pattern, wall_loops, top_shell_layers, bottom_shell_layers, support.enable, speed.perimeter, speed.infill, bed_temperature, nozzle_temperature, filament_type, brim.enable

Key Tier 2 candidates:
- All remaining speed fields, cooling settings, retraction settings, seam position, wall_order, line widths, bridge settings
  </action>
  <verify>
    <automated>test -f /home/steve/libslic3r-rs/designDocs/TIER_MAP.md && grep -c "| " /home/steve/libslic3r-rs/designDocs/TIER_MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - designDocs/TIER_MAP.md exists
    - Contains "Tier 1" and "Tier 2" and "Tier 3" and "Tier 4" text
    - Contains at least 380 table rows (one per field)
    - Contains "Summary" or "Counts" section with per-tier totals
    - Contains category grouping headers (Speed, Quality, Cooling, etc.)
    - Tier 1 count is between 10-25
    - Tier 2 count is between 40-80
  </acceptance_criteria>
  <done>TIER_MAP.md contains all ~387 fields with tier assignments, grouped by category with rationale</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User review of tier assignments</name>
  <files>designDocs/TIER_MAP.md</files>
  <action>
Present TIER_MAP.md for user review. The user must approve tier assignments before annotation plans (35-04, 35-05) can execute. This is a blocking gate.

Steps for verification:
1. Open designDocs/TIER_MAP.md
2. Review Tier 1 (~15 fields) -- are these the most essential settings for a beginner?
3. Review Tier 2 (~60 fields) -- intermediate control level makes sense?
4. Spot-check Tier 3 vs Tier 4 boundary -- are niche/debug fields properly in Tier 4?
5. Check category assignments match field purpose
  </action>
  <verify>
    <automated>test -f /home/steve/libslic3r-rs/designDocs/TIER_MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - User has reviewed TIER_MAP.md and typed "approved" or provided feedback
    - If feedback provided, changes have been applied to TIER_MAP.md
  </acceptance_criteria>
  <done>User has approved tier assignments in TIER_MAP.md</done>
</task>

</tasks>

<verification>
- TIER_MAP.md exists with complete field listing
- User has approved tier assignments
</verification>

<success_criteria>
- All ~387 fields documented with tier, category, and rationale
- Tier distribution roughly matches targets (15/60/200/rest)
- User review gate passed
</success_criteria>

<output>
After completion, create `.planning/phases/35-configschema-system-with-setting-metadata-and-json-schema-generation/35-03-SUMMARY.md`
</output>
