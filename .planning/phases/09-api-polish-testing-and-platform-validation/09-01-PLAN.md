---
phase: 09-api-polish-testing-and-platform-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/slicecore-engine/src/lib.rs
  - crates/slicecore-engine/src/engine.rs
  - crates/slicecore-engine/src/gcode_gen.rs
  - crates/slicecore-engine/src/polyhole.rs
  - crates/slicecore-mesh/src/repair.rs
  - crates/slicecore-math/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "cargo doc --no-deps --workspace produces zero warnings"
    - "All 6 known broken intra-doc links are fixed"
  artifacts:
    - path: "crates/slicecore-engine/src/engine.rs"
      provides: "Fixed Engine::with_plugin_registry doc link"
      contains: "with_plugin_registry"
    - path: "crates/slicecore-engine/src/gcode_gen.rs"
      provides: "Fixed GcodeWriter doc link"
      contains: "GcodeWriter"
  key_links:
    - from: "crates/slicecore-engine/src/engine.rs"
      to: "rustdoc"
      via: "intra-doc links resolve correctly"
      pattern: "\\[`?\\w+`?\\]"
---

<objective>
Fix all 6 known rustdoc warnings identified in research: broken intra-doc links in engine.rs, gcode_gen.rs, polyhole.rs, lib.rs, and repair.rs.

Purpose: API-01 requires clean rustdoc output. This plan eliminates all existing warnings so downstream plans can add docs on a clean baseline.
Output: Zero rustdoc warnings across the entire workspace.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-api-polish-testing-and-platform-validation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all rustdoc warnings across workspace</name>
  <files>
    crates/slicecore-engine/src/engine.rs
    crates/slicecore-engine/src/gcode_gen.rs
    crates/slicecore-engine/src/polyhole.rs
    crates/slicecore-engine/src/lib.rs
    crates/slicecore-mesh/src/repair.rs
    crates/slicecore-math/src/lib.rs
  </files>
  <action>
    Fix all 6 known rustdoc warnings identified in research:
    1. Fix `Engine::with_plugin_registry` broken intra-doc link in engine.rs
    2. Fix `GcodeWriter` unresolved link in gcode_gen.rs
    3. Fix `PrintConfig` unresolved link in polyhole.rs
    4. Fix missing escape in engine lib.rs doc comment
    5. Fix `[1]` and `[2]` unescaped brackets in repair/normals.rs (in slicecore-mesh)
    6. Check slicecore-math/src/lib.rs for any additional warnings

    Run `cargo doc --no-deps --workspace 2>&1 | grep warning` to find all warnings. Fix each one. Iterate until zero warnings remain.

    Common fixes:
    - For broken intra-doc links: either fix the path (e.g., `[`Engine::with_plugin_registry`]` -> `[`Engine::with_plugin_registry`](crate::Engine::with_plugin_registry)`) or wrap in backticks without link syntax
    - For unescaped brackets `[1]`: use `\[1\]` or wrap in backticks
    - For unresolved type links: use full path or `[`Type`](crate::module::Type)`

    Do NOT add module-level docs or new doc comments -- this plan only fixes existing broken docs.
  </action>
  <verify>
    Run `cargo doc --no-deps --workspace 2>&1 | grep -c warning` returns 0.
    Run `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace` exits with code 0.
  </verify>
  <done>
    `cargo doc --no-deps --workspace` produces zero warnings. All intra-doc links resolve correctly.
  </done>
</task>

</tasks>

<verification>
1. `cargo doc --no-deps --workspace 2>&1 | grep warning` returns empty
2. `RUSTDOCFLAGS="-D warnings" cargo doc --no-deps --workspace` exits 0
3. `cargo build --workspace` still succeeds
</verification>

<success_criteria>
- Zero rustdoc warnings across all 11 crates
- All 6 known broken doc links fixed
- No regressions in build or tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-api-polish-testing-and-platform-validation/09-01-SUMMARY.md`
</output>
