---
phase: 09-api-polish-testing-and-platform-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/slicecore-engine/src/lib.rs
  - crates/slicecore-engine/src/toolpath.rs
  - crates/slicecore-engine/src/engine.rs
  - crates/slicecore-engine/src/perimeter.rs
  - crates/slicecore-engine/src/gcode_gen.rs
  - crates/slicecore-engine/src/support/mod.rs
  - crates/slicecore-engine/src/support/config.rs
  - crates/slicecore-engine/src/multimaterial.rs
  - crates/slicecore-engine/src/sequential.rs
  - crates/slicecore-engine/src/modifier.rs
  - crates/slicecore-engine/src/arachne.rs
  - crates/slicecore-engine/src/calibration.rs
  - crates/slicecore-slicer/src/lib.rs
  - crates/slicecore-mesh/src/lib.rs
  - crates/slicecore-mesh/src/repair.rs
  - crates/slicecore-math/src/lib.rs
  - crates/slicecore-geo/src/lib.rs
  - crates/slicecore-fileio/src/lib.rs
  - crates/slicecore-gcode-io/src/lib.rs
  - crates/slicecore-plugin/src/lib.rs
  - crates/slicecore-plugin-api/src/lib.rs
  - crates/slicecore-ai/src/lib.rs
autonomous: true

must_haves:
  truths:
    - "cargo doc --no-deps --workspace produces zero warnings"
    - "Every pub mod declaration has a module-level doc comment"
    - "SliceResult, FeatureType, ToolpathSegment, and LayerToolpath all derive Serialize and Deserialize"
    - "serde_json::to_string_pretty on a SliceResult succeeds without error"
  artifacts:
    - path: "crates/slicecore-engine/src/engine.rs"
      provides: "SliceResult with Serialize/Deserialize derives"
      contains: "derive.*Serialize.*Deserialize"
    - path: "crates/slicecore-engine/src/toolpath.rs"
      provides: "FeatureType, ToolpathSegment, LayerToolpath with Serialize/Deserialize"
      contains: "derive.*Serialize"
  key_links:
    - from: "crates/slicecore-engine/src/engine.rs"
      to: "serde"
      via: "derive macro"
      pattern: "#\\[derive.*Serialize"
---

<objective>
Fix all rustdoc warnings, add module-level doc comments to every `pub mod`, and add `Serialize`/`Deserialize` derives to all public API types that cross the boundary (SliceResult, FeatureType, ToolpathSegment, LayerToolpath, SupportResult, ContourPerimeters, etc.).

Purpose: API-01 requires all public items documented, API-03/04 require structured serialization. This plan makes the type graph serializable and the documentation clean.
Output: Zero rustdoc warnings, all public types serializable via serde.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-api-polish-testing-and-platform-validation/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix rustdoc warnings and add module-level doc comments</name>
  <files>
    crates/slicecore-engine/src/lib.rs
    crates/slicecore-engine/src/engine.rs
    crates/slicecore-engine/src/gcode_gen.rs
    crates/slicecore-engine/src/polyhole.rs
    crates/slicecore-mesh/src/repair.rs
    crates/slicecore-math/src/lib.rs
    crates/slicecore-geo/src/lib.rs
    crates/slicecore-fileio/src/lib.rs
    crates/slicecore-gcode-io/src/lib.rs
    crates/slicecore-slicer/src/lib.rs
    crates/slicecore-plugin/src/lib.rs
    crates/slicecore-plugin-api/src/lib.rs
    crates/slicecore-ai/src/lib.rs
  </files>
  <action>
    Fix all 6 known rustdoc warnings identified in research:
    1. Fix `Engine::with_plugin_registry` broken intra-doc link in engine.rs
    2. Fix `GcodeWriter` unresolved link in gcode_gen.rs
    3. Fix `PrintConfig` unresolved link in polyhole.rs
    4. Fix missing escape in engine lib.rs doc comment
    5. Fix `[1]` and `[2]` unescaped brackets in repair/normals.rs (in slicecore-mesh)

    Then add `//! Module-level documentation.` comments to every `pub mod` declaration across all 11 crates. The research found ~40 `pub mod` declarations lacking module-level docs. For each, add a brief one-line `//!` comment at the top of the module file (e.g., `//! Ironing pass generation for top surfaces.`).

    Run `cargo doc --no-deps --workspace 2>&1 | grep warning` to find all warnings. Fix each one. Iterate until zero warnings remain.

    Do NOT add doc comments to non-public modules or private items -- only `pub mod` and public items that are missing docs.
  </action>
  <verify>
    Run `cargo doc --no-deps --workspace 2>&1 | grep -c warning` returns 0.
    Run `cargo doc --no-deps --workspace` exits with code 0.
  </verify>
  <done>
    `cargo doc --no-deps --workspace` produces zero warnings. Every public module has a doc comment.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Serialize/Deserialize to all public API boundary types</name>
  <files>
    crates/slicecore-engine/src/engine.rs
    crates/slicecore-engine/src/toolpath.rs
    crates/slicecore-engine/src/perimeter.rs
    crates/slicecore-engine/src/support/mod.rs
    crates/slicecore-engine/src/support/config.rs
    crates/slicecore-engine/src/multimaterial.rs
    crates/slicecore-engine/src/sequential.rs
    crates/slicecore-engine/src/modifier.rs
    crates/slicecore-engine/src/arachne.rs
    crates/slicecore-engine/src/calibration.rs
    crates/slicecore-engine/src/gap_fill.rs
    crates/slicecore-engine/Cargo.toml
  </files>
  <action>
    Add `Serialize, Deserialize` derives to all public types that cross the API boundary. Key types:

    1. `SliceResult` in engine.rs -- add `#[derive(Serialize, Deserialize)]`. The `gcode: Vec<u8>` field should use `#[serde(with = "serde_bytes")]` or similar if needed for efficient serialization. Alternatively use `#[serde(skip)]` on the gcode field and keep it as raw bytes (users get gcode from the file, metadata from JSON).
    2. `FeatureType` enum in toolpath.rs -- add `#[derive(Serialize, Deserialize)]`
    3. `ToolpathSegment` in toolpath.rs -- add `#[derive(Serialize, Deserialize)]`
    4. `LayerToolpath` in toolpath.rs -- add `#[derive(Serialize, Deserialize)]`
    5. `ContourPerimeters` and `PerimeterShell` in perimeter.rs -- add if they are pub
    6. `SupportRegion` and `SupportResult` in support/mod.rs -- add derives
    7. `SupportConfig` in support/config.rs -- check if already has Serialize, add if not
    8. `PurgeTowerLayer`, `ToolChangeSequence` in multimaterial.rs
    9. `ObjectBounds` in sequential.rs
    10. `ModifierMesh`, `ModifierRegion` in modifier.rs
    11. `ArachnePerimeter`, `ArachneResult` in arachne.rs
    12. `GapFillPath` in gap_fill.rs

    For types with non-serializable fields (e.g., function pointers, OnceLock, complex borrowed types), use `#[serde(skip)]` on those fields.

    Add `serde_json` as a runtime dependency (not just dev-dependency) in slicecore-engine/Cargo.toml:
    ```toml
    serde_json = { workspace = true }
    ```

    After adding all derives, run `cargo build --workspace` to verify no compilation errors from transitive Serialize requirements.
  </action>
  <verify>
    Run `cargo build --workspace` succeeds.
    Run `cargo test --workspace` passes.
    Add a quick test in engine.rs tests: create a minimal SliceResult and call `serde_json::to_string(&result)` -- it should return Ok.
  </verify>
  <done>
    All public API types derive Serialize/Deserialize. `serde_json::to_string_pretty` works on SliceResult and all nested types.
  </done>
</task>

</tasks>

<verification>
1. `cargo doc --no-deps --workspace 2>&1 | grep warning` returns empty
2. `cargo build --workspace` succeeds
3. `cargo test --workspace` passes (including new serialization test)
4. `serde_json::to_string_pretty` works on a SliceResult instance
</verification>

<success_criteria>
- Zero rustdoc warnings across all 11 crates
- Every `pub mod` has a module-level doc comment
- SliceResult, FeatureType, ToolpathSegment, LayerToolpath all implement Serialize + Deserialize
- cargo test passes with serialization roundtrip test
</success_criteria>

<output>
After completion, create `.planning/phases/09-api-polish-testing-and-platform-validation/09-01-SUMMARY.md`
</output>
