---
phase: 20-expand-printconfig-field-coverage-and-profile-mapping
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - crates/slicecore-engine/src/profile_import.rs
autonomous: true
requirements:
  - "SC4-json-mapper"
  - "SC6-x1c-profiles"

must_haves:
  truths:
    - "JSON profile mapper maps at least 100 upstream fields to PrintConfig typed fields (up from ~43)"
    - "Unmapped upstream fields are stored in config.passthrough BTreeMap"
    - "extract_array_f64 helper extracts full Vec<f64> from JSON arrays and populates Vec<f64> fields (nozzle_diameters, jerk_values, temperatures)"
    - "Multi-extruder array fields populated as Vec<f64> -- nozzle_diameter, jerk, and temperature JSON arrays map to config.machine.nozzle_diameters, config.machine.jerk_values_x/y/z/e, config.filament.nozzle_temperatures, config.filament.bed_temperatures, etc."
    - "All upstream speed, line width, acceleration, cooling, machine, and filament fields are mapped"
    - "ImportResult.unmapped_fields is empty (all fields either typed or passthrough)"
  artifacts:
    - path: "crates/slicecore-engine/src/profile_import.rs"
      provides: "Expanded JSON field mapping (~120 match arms), extract_array_f64 helper, passthrough storage"
      contains: "config.speeds.bridge"
  key_links:
    - from: "crates/slicecore-engine/src/profile_import.rs"
      to: "config.rs sub-configs"
      via: "apply_field_mapping match arms"
      pattern: "config\\.speeds\\."
---

<objective>
Expand the JSON profile mapper (profile_import.rs) from ~43 mapped fields to 100+ fields, covering all OrcaSlicer/BambuStudio process, filament, and machine profile fields. Unmapped fields go into the passthrough BTreeMap. Add extract_array_f64 helper for multi-extruder array fields.

Purpose: Enables converted profiles to capture enough settings for meaningful slicer output comparison.
Output: Expanded profile_import.rs with comprehensive field mapping.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-RESEARCH.md
@.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-01-SUMMARY.md
@crates/slicecore-engine/src/profile_import.rs
@crates/slicecore-engine/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add extract_array_f64 helper and expand JSON field mapping</name>
  <files>crates/slicecore-engine/src/profile_import.rs</files>
  <action>
**1. Add extract_array_f64 helper function:**
```rust
/// Extract all values from a JSON array as f64 Vec.
/// Handles: Array of strings, array of numbers, single value, nil sentinels.
fn extract_array_f64(value: &serde_json::Value) -> Vec<f64> { ... }
```
See research 20-RESEARCH.md for exact implementation.

**2. Expand apply_field_mapping with new match arms organized by sub-config target:**

Add all of the following field mappings to the match statement in `apply_field_mapping`. Group with section comments.

**--- Speed sub-config fields ---**
- "bridge_speed" => parse_and_set_f64(value, &amp;mut config.speeds.bridge)
- "inner_wall_speed" => config.speeds.inner_wall
- "gap_infill_speed" => config.speeds.gap_fill
- "top_surface_speed" => config.speeds.top_surface
- "internal_solid_infill_speed" => config.speeds.internal_solid_infill
- "initial_layer_infill_speed" => config.speeds.initial_layer_infill
- "support_speed" => config.speeds.support
- "support_interface_speed" => config.speeds.support_interface
- "small_perimeter_speed" => config.speeds.small_perimeter (handle percentage format: strip % if present)
- "solid_infill_speed" => config.speeds.solid_infill
- "overhang_1_4_speed" | "overhang_speed_0" => config.speeds.overhang_1_4
- "overhang_2_4_speed" | "overhang_speed_1" => config.speeds.overhang_2_4
- "overhang_3_4_speed" | "overhang_speed_2" => config.speeds.overhang_3_4
- "overhang_4_4_speed" | "overhang_speed_3" => config.speeds.overhang_4_4
- "travel_speed_z" => config.speeds.travel_z

**--- Line width sub-config fields ---**
- "line_width" | "extrusion_width" => handle as base line width (store in config passthrough or a base field)
- "outer_wall_line_width" => config.line_widths.outer_wall
- "inner_wall_line_width" => config.line_widths.inner_wall
- "sparse_infill_line_width" => config.line_widths.infill
- "top_surface_line_width" => config.line_widths.top_surface
- "initial_layer_line_width" => config.line_widths.initial_layer
- "internal_solid_infill_line_width" => config.line_widths.internal_solid_infill
- "support_line_width" => config.line_widths.support

**--- Cooling sub-config fields ---**
- "fan_max_speed" => config.cooling.fan_max_speed (percentage, strip % if present)
- "fan_min_speed" => config.cooling.fan_min_speed
- "slow_down_layer_time" | "slowdown_below_layer_time" => config.cooling.slow_down_layer_time
- "slow_down_min_speed" | "min_print_speed" => config.cooling.slow_down_min_speed
- "overhang_fan_speed" => config.cooling.overhang_fan_speed
- "overhang_fan_threshold" => config.cooling.overhang_fan_threshold
- "full_fan_speed_layer" | "disable_fan_first_layers" => config.cooling.full_fan_speed_layer (for disable_fan_first_layers, it's the same concept inverted)
- "slow_down_for_layer_cooling" => parse_and_set_bool for config.cooling.slow_down_for_layer_cooling

**--- Retraction sub-config fields ---**
- "deretraction_speed" => config.retraction.deretraction_speed
- "retract_before_wipe" => config.retraction.retract_before_wipe (percentage)
- "retract_when_changing_layer" => config.retraction.retract_when_changing_layer (bool)
- "wipe" => config.retraction.wipe (bool)
- "wipe_distance" => config.retraction.wipe_distance

**--- Machine sub-config fields ---**
- "machine_start_gcode" | "start_gcode" => config.machine.start_gcode = value.to_string(); true
- "machine_end_gcode" | "end_gcode" => config.machine.end_gcode = value.to_string(); true
- "layer_change_gcode" | "layer_gcode" => config.machine.layer_change_gcode = value.to_string(); true
- "printable_height" | "max_print_height" => config.machine.printable_height
- "machine_max_acceleration_x" => config.machine.max_acceleration_x
- "machine_max_acceleration_y" => config.machine.max_acceleration_y
- "machine_max_acceleration_z" => config.machine.max_acceleration_z
- "machine_max_acceleration_e" => config.machine.max_acceleration_e
- "machine_max_acceleration_extruding" => config.machine.max_acceleration_extruding
- "machine_max_acceleration_retracting" => config.machine.max_acceleration_retracting
- "machine_max_acceleration_travel" => config.machine.max_acceleration_travel
- "machine_max_speed_x" => config.machine.max_speed_x
- "machine_max_speed_y" => config.machine.max_speed_y
- "machine_max_speed_z" => config.machine.max_speed_z
- "machine_max_speed_e" => config.machine.max_speed_e
- "nozzle_type" => config.machine.nozzle_type = value.to_string(); true
- "printer_model" | "printer_model_id" => config.machine.printer_model = value.to_string(); true
- "bed_shape" | "printable_area" => config.machine.bed_shape = value.to_string(); true
- "min_layer_height" => config.machine.min_layer_height
- "max_layer_height" => config.machine.max_layer_height

**--- Multi-extruder Vec<f64> array fields (per user decision) ---**
Use `extract_array_f64(value)` to populate these Vec<f64> fields:
- "nozzle_diameter" => config.machine.nozzle_diameters = extract_array_f64(value); true
- "machine_max_jerk_x" => config.machine.jerk_values_x = extract_array_f64(value); true
- "machine_max_jerk_y" => config.machine.jerk_values_y = extract_array_f64(value); true
- "machine_max_jerk_z" => config.machine.jerk_values_z = extract_array_f64(value); true
- "machine_max_jerk_e" => config.machine.jerk_values_e = extract_array_f64(value); true
- "nozzle_temperature" | "temperature" => config.filament.nozzle_temperatures = extract_array_f64(value); true
- "bed_temperature" | "hot_plate_temp" => config.filament.bed_temperatures = extract_array_f64(value); true
- "nozzle_temperature_initial_layer" | "first_layer_temperature" => config.filament.first_layer_nozzle_temperatures = extract_array_f64(value); true
- "bed_temperature_initial_layer" | "first_layer_bed_temperature" | "hot_plate_temp_initial_layer" => config.filament.first_layer_bed_temperatures = extract_array_f64(value); true

**--- Acceleration sub-config fields ---**
- "outer_wall_acceleration" => config.accel.outer_wall
- "inner_wall_acceleration" => config.accel.inner_wall
- "initial_layer_acceleration" => config.accel.initial_layer
- "initial_layer_travel_acceleration" | "initial_layer_travel_speed" => config.accel.initial_layer_travel
- "top_surface_acceleration" => config.accel.top_surface
- "sparse_infill_acceleration" => config.accel.sparse_infill
- "bridge_acceleration" => config.accel.bridge

**--- Filament sub-config fields ---**
- "filament_type" => config.filament.filament_type = value.to_string(); true
- "filament_vendor" => config.filament.filament_vendor = value.to_string(); true
- "filament_max_volumetric_speed" => config.filament.max_volumetric_speed
- "nozzle_temperature_range_low" => config.filament.nozzle_temperature_range_low
- "nozzle_temperature_range_high" => config.filament.nozzle_temperature_range_high
- "filament_retraction_length" => config.filament.filament_retraction_length = Some(parsed_value)
- "filament_retraction_speed" => config.filament.filament_retraction_speed = Some(parsed_value)
- "filament_start_gcode" => config.filament.filament_start_gcode = value.to_string(); true
- "filament_end_gcode" => config.filament.filament_end_gcode = value.to_string(); true

**--- Process misc flat fields ---**
- "bridge_flow" | "bridge_flow_ratio" => config.bridge_flow
- "elefant_foot_compensation" => config.elefant_foot_compensation
- "infill_direction" => config.infill_direction
- "infill_wall_overlap" | "infill_overlap" => config.infill_wall_overlap (handle percentage format)
- "spiral_mode" | "spiral_vase" => config.spiral_mode (bool)
- "only_one_wall_top" => config.only_one_wall_top (bool)
- "resolution" => config.resolution
- "raft_layers" => config.raft_layers (parse as u32)
- "detect_thin_wall" | "thin_walls" => config.detect_thin_wall (bool)

**3. Change the default match arm to store unmapped fields in passthrough:**
Replace the current `_ => false` (or similar) default arm with:
```rust
_ => {
    // Store unmapped fields in passthrough for round-trip fidelity
    config.passthrough.insert(key.to_string(), value.to_string());
    true // Considered "handled" via passthrough
}
```

Update `import_upstream_profile` accordingly: since all fields are now "handled" (either typed or passthrough), the unmapped_fields Vec becomes a list of passthrough fields. Consider renaming or adjusting ImportResult field semantics: `unmapped_fields` now contains passthrough field names instead of truly unmapped ones. Alternatively, add a `passthrough_fields: Vec<String>` field to ImportResult.

**4. Add unit tests:**
- Test that new speed fields map correctly from JSON
- Test that new line width fields map correctly
- Test that machine gcode string fields store correctly
- Test that unknown fields go into passthrough
- Test that extract_array_f64 handles arrays, singles, and nils
- Test that nozzle_diameter JSON array `["0.4", "0.6"]` populates config.machine.nozzle_diameters as vec![0.4, 0.6]
- Test that temperature JSON array populates config.filament.nozzle_temperatures as full Vec<f64>
- Test that jerk JSON arrays populate config.machine.jerk_values_x/y/z/e as full Vec<f64>
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine profile_import::tests -- --nocapture 2>&amp;1 | tail -30</automated>
    <manual>Verify new match arm count exceeds 100 total mapped fields</manual>
  </verify>
  <done>
    - apply_field_mapping has 100+ match arms covering all major upstream field categories
    - extract_array_f64 helper exists and handles all JSON value types
    - Default match arm stores unmapped fields in config.passthrough
    - All existing profile_import tests pass unchanged
    - New unit tests verify speed, line width, machine, cooling, and passthrough mapping
  </done>
</task>

</tasks>

<verification>
```bash
# Profile import tests pass
cargo test -p slicecore-engine profile_import::tests

# Full workspace compiles
cargo check --workspace

# Clippy clean
cargo clippy --workspace -- -D warnings
```
</verification>

<success_criteria>
- JSON mapper handles 100+ upstream fields (up from ~43)
- Unmapped fields stored in passthrough BTreeMap
- extract_array_f64 helper works for multi-value extraction
- All existing and new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-02-SUMMARY.md`
</output>
