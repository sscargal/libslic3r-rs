---
phase: 20-expand-printconfig-field-coverage-and-profile-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/slicecore-engine/src/config.rs
autonomous: true
requirements:
  - "SC1-process-fields"
  - "SC2-machine-fields"
  - "SC3-filament-fields"

must_haves:
  truths:
    - "PrintConfig has nested sub-config structs for line widths, speeds, cooling, retraction, machine, acceleration, and filament properties"
    - "All sub-config structs derive Debug, Clone, Serialize, Deserialize with serde(default) and sensible Default impls"
    - "PrintConfig has a BTreeMap<String, String> passthrough field for unmapped upstream fields"
    - "Existing flat fields remain untouched -- all existing tests and engine code compiles unchanged"
    - "TOML deserialization of new nested sections works (e.g., [line_widths], [speeds], [cooling])"
  artifacts:
    - path: "crates/slicecore-engine/src/config.rs"
      provides: "LineWidthConfig, SpeedConfig, CoolingConfig, RetractionConfig, MachineConfig, AccelerationConfig, FilamentPropsConfig sub-config structs plus passthrough BTreeMap"
      contains: "pub struct LineWidthConfig"
  key_links:
    - from: "crates/slicecore-engine/src/config.rs"
      to: "PrintConfig"
      via: "nested struct fields"
      pattern: "pub line_widths: LineWidthConfig"
---

<objective>
Add nested sub-config structs to PrintConfig for the ~86 new typed fields identified by research, plus a BTreeMap passthrough for unmapped upstream fields. This plan does NOT migrate existing flat fields -- it only adds NEW structure alongside them. This ensures zero breaking changes: all existing engine code and tests compile unchanged.

Purpose: Provides the data model foundation that Plans 02-03 (mapper expansion) and Plan 04 (field migration) will build on.
Output: Expanded config.rs with 7 new sub-config structs and passthrough storage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-RESEARCH.md
@crates/slicecore-engine/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sub-config structs and passthrough to PrintConfig</name>
  <files>crates/slicecore-engine/src/config.rs</files>
  <action>
Add the following sub-config structs to config.rs, each with `#[derive(Debug, Clone, Serialize, Deserialize)]` and `#[serde(default)]` and a custom `Default` impl with sensible industry-standard defaults (use BambuStudio defaults as reference where applicable):

**1. LineWidthConfig** (field name: `line_widths`)
- outer_wall: f64 (default 0.42)
- inner_wall: f64 (default 0.45)
- infill: f64 (default 0.45)
- top_surface: f64 (default 0.42)
- initial_layer: f64 (default 0.5)
- internal_solid_infill: f64 (default 0.42)
- support: f64 (default 0.42)

**2. SpeedConfig** (field name: `speeds`)
- bridge: f64 (default 25.0 mm/s)
- inner_wall: f64 (default 0.0, meaning inherit from perimeter_speed)
- gap_fill: f64 (default 0.0, meaning inherit from perimeter_speed)
- top_surface: f64 (default 0.0, meaning inherit from perimeter_speed)
- internal_solid_infill: f64 (default 0.0)
- initial_layer_infill: f64 (default 0.0)
- support: f64 (default 0.0)
- support_interface: f64 (default 0.0)
- small_perimeter: f64 (default 0.0)
- solid_infill: f64 (default 0.0)
- overhang_1_4: f64 (default 0.0, speed for 0-25% overhang)
- overhang_2_4: f64 (default 0.0, speed for 25-50% overhang)
- overhang_3_4: f64 (default 0.0, speed for 50-75% overhang)
- overhang_4_4: f64 (default 0.0, speed for 75-100% overhang)
- travel_z: f64 (default 0.0 mm/s, Z-axis travel speed)

**3. CoolingConfig** (field name: `cooling`)
- fan_max_speed: f64 (default 100.0, percentage 0-100)
- fan_min_speed: f64 (default 35.0, percentage 0-100)
- slow_down_layer_time: f64 (default 5.0, seconds)
- slow_down_min_speed: f64 (default 10.0, mm/s)
- overhang_fan_speed: f64 (default 100.0, percentage)
- overhang_fan_threshold: f64 (default 25.0, degrees)
- full_fan_speed_layer: u32 (default 0, layer number for full fan)
- slow_down_for_layer_cooling: bool (default true)

**4. RetractionConfig** (field name: `retraction`)
Note: Do NOT move the existing flat retract_* fields yet (that happens in Plan 04). These are ADDITIONAL retraction fields not currently in PrintConfig.
- deretraction_speed: f64 (default 0.0, 0 = same as retraction speed)
- retract_before_wipe: f64 (default 0.0, percentage of retraction before wipe)
- retract_when_changing_layer: bool (default false)
- wipe: bool (default false)
- wipe_distance: f64 (default 0.0, mm)

**5. MachineConfig** (field name: `machine`)
- printable_height: f64 (default 250.0, mm)
- max_acceleration_x: f64 (default 5000.0)
- max_acceleration_y: f64 (default 5000.0)
- max_acceleration_z: f64 (default 100.0)
- max_acceleration_e: f64 (default 5000.0)
- max_acceleration_extruding: f64 (default 5000.0)
- max_acceleration_retracting: f64 (default 5000.0)
- max_acceleration_travel: f64 (default 5000.0)
- max_speed_x: f64 (default 500.0)
- max_speed_y: f64 (default 500.0)
- max_speed_z: f64 (default 12.0)
- max_speed_e: f64 (default 120.0)
- start_gcode: String (default empty)
- end_gcode: String (default empty)
- layer_change_gcode: String (default empty)
- nozzle_type: String (default empty)
- printer_model: String (default empty)
- bed_shape: String (default empty, serialized polygon or rect descriptor)
- min_layer_height: f64 (default 0.07)
- max_layer_height: f64 (default 0.0, 0 = auto from nozzle)

**6. AccelerationConfig** (field name: `accel`)
Note: Do NOT move existing print_acceleration, travel_acceleration fields yet. These are ADDITIONAL per-feature acceleration fields.
- outer_wall: f64 (default 0.0, 0 = use print_acceleration)
- inner_wall: f64 (default 0.0)
- initial_layer: f64 (default 0.0)
- initial_layer_travel: f64 (default 0.0)
- top_surface: f64 (default 0.0)
- sparse_infill: f64 (default 0.0)
- bridge: f64 (default 0.0)

**7. FilamentPropsConfig** (field name: `filament`)
Note: Do NOT move existing filament_density, filament_cost_per_kg, filament_diameter fields yet. These are ADDITIONAL filament properties.
- filament_type: String (default empty, e.g., "PLA", "ABS", "PETG")
- filament_vendor: String (default empty)
- max_volumetric_speed: f64 (default 0.0, 0 = unlimited)
- nozzle_temperature_range_low: f64 (default 190.0)
- nozzle_temperature_range_high: f64 (default 240.0)
- filament_retraction_length: Option&lt;f64&gt; (default None, overrides retract_length when set)
- filament_retraction_speed: Option&lt;f64&gt; (default None)
- filament_start_gcode: String (default empty)
- filament_end_gcode: String (default empty)

**8. Passthrough field on PrintConfig:**
Add `use std::collections::BTreeMap;` import and add field:
```rust
/// Passthrough fields from upstream profiles that have no engine equivalent.
/// Preserved for round-trip fidelity and G-code template variable access.
/// Uses BTreeMap for deterministic serialization order.
#[serde(default, skip_serializing_if = "BTreeMap::is_empty")]
pub passthrough: BTreeMap<String, String>,
```
Initialize as `BTreeMap::new()` in Default impl.

**9. Process misc fields on PrintConfig (flat, not nested):**
Add these directly to PrintConfig struct body:
- bridge_flow: f64 (default 1.0)
- elefant_foot_compensation: f64 (default 0.0, mm)
- infill_direction: f64 (default 45.0, degrees)
- infill_wall_overlap: f64 (default 0.15, fraction 0-1)
- spiral_mode: bool (default false, aka vase mode)
- only_one_wall_top: bool (default false)
- resolution: f64 (default 0.012, mm)
- raft_layers: u32 (default 0)
- detect_thin_wall: bool (default true)
Initialize with defaults in Default impl.

**10. Unit tests:**
Add tests for:
- `sub_config_defaults()` -- verify all sub-config structs have expected default values
- `sub_config_from_toml()` -- parse a TOML string with nested sections and verify values
- `passthrough_round_trip()` -- verify BTreeMap serializes/deserializes correctly
- `existing_tests_still_pass()` -- no explicit test needed; existing tests will be run

Ensure all existing tests in config.rs still pass with no changes (the new fields have defaults, so old TOML strings still parse correctly).
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine config::tests -- --nocapture 2>&amp;1 | tail -20</automated>
    <manual>Check that all existing config tests pass and new sub-config tests pass</manual>
  </verify>
  <done>
    - 7 new sub-config structs exist in config.rs with Debug/Clone/Serialize/Deserialize/Default
    - PrintConfig has line_widths, speeds, cooling, retraction, machine, accel, filament, passthrough fields
    - 9 new flat process misc fields on PrintConfig
    - All existing config tests pass unchanged
    - New unit tests for sub-config defaults, TOML parsing, and passthrough round-trip pass
  </done>
</task>

</tasks>

<verification>
```bash
# All config tests pass
cargo test -p slicecore-engine config::tests

# Full workspace compiles (no breaking changes)
cargo check --workspace

# Clippy clean
cargo clippy --workspace -- -D warnings
```
</verification>

<success_criteria>
- 7 sub-config structs added to config.rs
- BTreeMap passthrough field on PrintConfig
- 9 new flat process misc fields on PrintConfig
- All existing tests pass with zero changes
- Full workspace compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-01-SUMMARY.md`
</output>
