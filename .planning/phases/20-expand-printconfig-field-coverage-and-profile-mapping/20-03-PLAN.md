---
phase: 20-expand-printconfig-field-coverage-and-profile-mapping
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - crates/slicecore-engine/src/profile_import_ini.rs
autonomous: true
requirements:
  - "SC5-ini-mapper"

must_haves:
  truths:
    - "INI profile mapper maps the same expanded field set as the JSON mapper"
    - "PrusaSlicer-specific field names (perimeter_speed, first_layer_speed_over_raft, etc.) map to correct sub-config fields"
    - "Unmapped PrusaSlicer fields are stored in config.passthrough BTreeMap"
    - "Multi-extruder comma-separated values (nozzle_diameter, jerk, temperatures) parsed into Vec<f64> fields via parse_comma_separated_f64 helper"
    - "All existing INI profile import tests pass unchanged"
  artifacts:
    - path: "crates/slicecore-engine/src/profile_import_ini.rs"
      provides: "Expanded PrusaSlicer field mapping (~100 match arms), passthrough storage"
      contains: "config.speeds.bridge"
  key_links:
    - from: "crates/slicecore-engine/src/profile_import_ini.rs"
      to: "config.rs sub-configs"
      via: "apply_prusaslicer_field_mapping match arms"
      pattern: "config\\.line_widths\\."
---

<objective>
Expand the PrusaSlicer INI profile mapper (profile_import_ini.rs) to match the JSON mapper's expanded coverage, mapping all PrusaSlicer field names to the new sub-config fields. Unmapped fields go to passthrough. PrusaSlicer uses different field names from OrcaSlicer (e.g., "perimeter_speed" vs "outer_wall_speed"), so the match arms use PrusaSlicer-specific key names.

Purpose: Ensures all 4 profile sources (OrcaSlicer, BambuStudio, PrusaSlicer, CrealityPrint) benefit equally from expanded field coverage.
Output: Expanded profile_import_ini.rs with comprehensive PrusaSlicer field mapping.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-RESEARCH.md
@.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-01-SUMMARY.md
@crates/slicecore-engine/src/profile_import_ini.rs
@crates/slicecore-engine/src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand PrusaSlicer INI field mapping</name>
  <files>crates/slicecore-engine/src/profile_import_ini.rs</files>
  <action>
Expand `apply_prusaslicer_field_mapping` with new match arms organized by sub-config target. PrusaSlicer uses its own field naming convention (different from OrcaSlicer), so map PrusaSlicer names to the same PrintConfig sub-config fields.

**--- Speed sub-config fields (PrusaSlicer names) ---**
- "bridge_speed" => config.speeds.bridge
- "perimeter_speed" already mapped to config.perimeter_speed -- keep existing
- "small_perimeter_speed" => config.speeds.small_perimeter (handle % format: skip if percentage, parse if absolute)
- "external_perimeter_speed" => this is OrcaSlicer's "outer_wall_speed" equivalent -- already mapped
- "gap_fill_speed" => config.speeds.gap_fill
- "top_solid_infill_speed" | "top_infill_speed" => config.speeds.top_surface
- "solid_infill_speed" => config.speeds.solid_infill
- "infill_speed" already mapped to config.infill_speed -- keep existing
- "support_material_speed" => config.speeds.support
- "support_material_interface_speed" => config.speeds.support_interface
- "first_layer_speed_over_raft" => passthrough (no direct equivalent)
- "travel_speed_z" => config.speeds.travel_z

**--- Line width sub-config fields (PrusaSlicer names) ---**
- "extrusion_width" => passthrough (base extrusion width)
- "first_layer_extrusion_width" => config.line_widths.initial_layer
- "perimeter_extrusion_width" => config.line_widths.outer_wall (PrusaSlicer does not distinguish inner/outer width)
- "external_perimeter_extrusion_width" => config.line_widths.outer_wall
- "infill_extrusion_width" => config.line_widths.infill
- "solid_infill_extrusion_width" => config.line_widths.internal_solid_infill
- "top_infill_extrusion_width" => config.line_widths.top_surface
- "support_material_extrusion_width" => config.line_widths.support

**--- Cooling sub-config fields (PrusaSlicer names) ---**
- "max_fan_speed" => config.cooling.fan_max_speed
- "min_fan_speed" => config.cooling.fan_min_speed
- "slowdown_below_layer_time" => config.cooling.slow_down_layer_time
- "min_print_speed" => config.cooling.slow_down_min_speed
- "bridge_fan_speed" => config.cooling.overhang_fan_speed (PrusaSlicer bridge fan is closest equivalent)
- "full_fan_speed_layer" | "disable_fan_first_layers" => config.cooling.full_fan_speed_layer
- "fan_always_on" => passthrough (PrusaSlicer-specific)
- "cooling" => config.cooling.slow_down_for_layer_cooling (bool, 0/1)

**--- Retraction sub-config fields (PrusaSlicer names) ---**
- "deretract_speed" => config.retraction.deretraction_speed
- "retract_before_wipe" => config.retraction.retract_before_wipe (percentage)
- "retract_layer_change" => config.retraction.retract_when_changing_layer (bool)
- "wipe" => config.retraction.wipe (bool)
- "wipe_distance" does not exist in PrusaSlicer -- skip

**--- Machine sub-config fields (PrusaSlicer names) ---**
- "start_gcode" => config.machine.start_gcode
- "end_gcode" => config.machine.end_gcode
- "layer_gcode" => config.machine.layer_change_gcode
- "max_print_height" => config.machine.printable_height
- "machine_max_acceleration_x" => config.machine.max_acceleration_x
- "machine_max_acceleration_y" => config.machine.max_acceleration_y
- "machine_max_acceleration_z" => config.machine.max_acceleration_z
- "machine_max_acceleration_e" => config.machine.max_acceleration_e
- "machine_max_acceleration_extruding" => config.machine.max_acceleration_extruding
- "machine_max_acceleration_retracting" => config.machine.max_acceleration_retracting
- "machine_max_acceleration_travel" => config.machine.max_acceleration_travel
- "machine_max_speed_x" | "machine_max_feedrate_x" => config.machine.max_speed_x
- "machine_max_speed_y" | "machine_max_feedrate_y" => config.machine.max_speed_y
- "machine_max_speed_z" | "machine_max_feedrate_z" => config.machine.max_speed_z
- "machine_max_speed_e" | "machine_max_feedrate_e" => config.machine.max_speed_e
- "nozzle_type" => config.machine.nozzle_type
- "printer_model" => config.machine.printer_model
- "bed_shape" => config.machine.bed_shape (PrusaSlicer format: "0x0,250x0,250x210,0x210")
- "min_layer_height" => config.machine.min_layer_height (handle comma-separated: take first)
- "max_layer_height" => config.machine.max_layer_height (handle comma-separated: take first)

**--- Multi-extruder Vec<f64> array fields (PrusaSlicer uses comma-separated values) ---**
PrusaSlicer stores multi-extruder values as comma-separated strings (e.g., "0.4,0.6"). Parse these into Vec<f64>:
Add a helper `fn parse_comma_separated_f64(value: &str) -> Vec<f64>` that splits on comma, trims, and parses each as f64.
- "nozzle_diameter" => config.machine.nozzle_diameters = parse_comma_separated_f64(value); true
- "machine_max_jerk_x" => config.machine.jerk_values_x = parse_comma_separated_f64(value); true
- "machine_max_jerk_y" => config.machine.jerk_values_y = parse_comma_separated_f64(value); true
- "machine_max_jerk_z" => config.machine.jerk_values_z = parse_comma_separated_f64(value); true
- "machine_max_jerk_e" => config.machine.jerk_values_e = parse_comma_separated_f64(value); true
- "temperature" => config.filament.nozzle_temperatures = parse_comma_separated_f64(value); true
- "bed_temperature" => config.filament.bed_temperatures = parse_comma_separated_f64(value); true
- "first_layer_temperature" => config.filament.first_layer_nozzle_temperatures = parse_comma_separated_f64(value); true
- "first_layer_bed_temperature" => config.filament.first_layer_bed_temperatures = parse_comma_separated_f64(value); true

**--- Acceleration sub-config fields (PrusaSlicer names) ---**
- "external_perimeter_acceleration" => config.accel.outer_wall (PrusaSlicer 2.7+)
- "perimeter_acceleration" => config.accel.inner_wall
- "first_layer_acceleration" | "first_layer_acceleration_over_raft" => config.accel.initial_layer
- "top_solid_infill_acceleration" => config.accel.top_surface
- "infill_acceleration" => config.accel.sparse_infill
- "bridge_acceleration" => config.accel.bridge

**--- Filament sub-config fields (PrusaSlicer names) ---**
- "filament_type" => config.filament.filament_type (handle comma-separated: take first)
- "filament_vendor" => config.filament.filament_vendor
- "filament_max_volumetric_speed" => config.filament.max_volumetric_speed (handle comma-separated)
- "temperature_range_low" | "min_temperature" => config.filament.nozzle_temperature_range_low
- "temperature_range_high" | "max_temperature" => config.filament.nozzle_temperature_range_high
- "filament_retract_length" => config.filament.filament_retraction_length = Some(val) (handle comma-separated)
- "filament_retract_speed" => config.filament.filament_retraction_speed = Some(val)
- "start_filament_gcode" => config.filament.filament_start_gcode
- "end_filament_gcode" => config.filament.filament_end_gcode

**--- Process misc flat fields (PrusaSlicer names) ---**
- "bridge_flow_ratio" => config.bridge_flow
- "elefant_foot_compensation" | "elephant_foot_compensation" => config.elefant_foot_compensation
- "fill_angle" => config.infill_direction
- "infill_overlap" => config.infill_wall_overlap (handle percentage format)
- "spiral_vase" => config.spiral_mode (bool)
- "only_one_perimeter_top" => config.only_one_wall_top (bool)
- "resolution" => config.resolution
- "raft_layers" => config.raft_layers
- "thin_walls" | "detect_thin_wall" => config.detect_thin_wall (bool)

**--- Change default arm to store passthrough ---**
Same pattern as JSON mapper: unmapped fields go to `config.passthrough.insert(key, value)`.

**--- Add unit tests ---**
- Test PrusaSlicer speed field mapping
- Test PrusaSlicer line width field mapping
- Test PrusaSlicer machine gcode string fields
- Test passthrough storage for unknown fields
- Test comma-separated value handling (take first for scalar fields)
- Test that nozzle_diameter comma-separated value "0.4,0.6" populates config.machine.nozzle_diameters as vec![0.4, 0.6]
- Test that temperature comma-separated value "200,210" populates config.filament.nozzle_temperatures as vec![200.0, 210.0]
- Test that jerk comma-separated values populate config.machine.jerk_values_x/y/z/e as full Vec<f64>
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine profile_import_ini::tests -- --nocapture 2>&amp;1 | tail -30</automated>
    <manual>Verify new match arms cover all major PrusaSlicer field categories</manual>
  </verify>
  <done>
    - apply_prusaslicer_field_mapping has 80+ match arms covering all major field categories
    - Default arm stores unmapped fields in config.passthrough
    - Existing INI import tests pass unchanged
    - New unit tests verify PrusaSlicer-specific field name mapping
  </done>
</task>

</tasks>

<verification>
```bash
# INI profile import tests pass
cargo test -p slicecore-engine profile_import_ini::tests

# Full workspace compiles
cargo check --workspace

# Clippy clean
cargo clippy --workspace -- -D warnings
```
</verification>

<success_criteria>
- INI mapper handles 80+ PrusaSlicer field names
- Same sub-config targets as JSON mapper
- Passthrough storage for unmapped PrusaSlicer fields
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/20-expand-printconfig-field-coverage-and-profile-mapping/20-03-SUMMARY.md`
</output>
