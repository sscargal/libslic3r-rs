---
phase: 22-migrate-from-lib3mf-to-lib3mf-core-ecosystem
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - .github/workflows/ci.yml
  - crates/slicecore-fileio/tests/wasm_3mf_test.rs
autonomous: true
requirements:
  - MESH-02
  - FOUND-01
  - FOUND-03

must_haves:
  truths:
    - "3MF parsing compiles successfully for wasm32-unknown-unknown target"
    - "3MF parsing compiles successfully for wasm32-wasip2 target"
    - "CI WASM build step includes slicecore-fileio (no longer excluded)"
    - "Full workspace test suite passes with no regressions"
    - "No C/C++ dependencies remain in slicecore-fileio dependency tree"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "WASM CI steps that prove 3MF works on WASM targets"
    - path: "crates/slicecore-fileio/tests/wasm_3mf_test.rs"
      provides: "Integration test proving 3MF parsing works end-to-end"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "crates/slicecore-fileio"
      via: "WASM build step includes slicecore-fileio"
      pattern: "cargo build --target.*wasm32"
---

<objective>
Verify that the lib3mf-core migration enables WASM compilation for 3MF and add CI validation. Add an integration test proving 3MF parsing works end-to-end, verify no C dependencies remain, and update CI if needed.

Purpose: Proves the migration achieved its primary goal (WASM 3MF support) and locks in the behavior with CI enforcement.
Output: WASM-verified 3MF support with CI enforcement and integration tests.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-migrate-from-lib3mf-to-lib3mf-core-ecosystem/22-RESEARCH.md
@.planning/phases/22-migrate-from-lib3mf-to-lib3mf-core-ecosystem/22-01-SUMMARY.md
@.github/workflows/ci.yml
@crates/slicecore-fileio/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify WASM compilation and add integration test</name>
  <files>
    crates/slicecore-fileio/tests/wasm_3mf_test.rs
  </files>
  <action>
1. Verify WASM compilation succeeds locally for slicecore-fileio:
   - Run `cargo build --target wasm32-unknown-unknown -p slicecore-fileio` -- must succeed
   - Run `cargo build --target wasm32-wasip2 -p slicecore-fileio` -- must succeed
   - If either fails, debug and fix the lib3mf-core dependency issue (likely a missing `default-features = false` or a transitive dependency pulling in a C library)

2. Verify no C dependencies remain in slicecore-fileio:
   - Run `cargo tree -p slicecore-fileio` and check that NO crate depends on a `-sys` crate (especially `zstd-sys`, `libz-sys`, or any other C library wrapper)
   - The old lib3mf pulled in `zip` (default features) -> `zstd-sys`. lib3mf-core uses `zip = { default-features = false, features = ["deflate"] }` which is pure Rust.

3. Create an integration test file `crates/slicecore-fileio/tests/wasm_3mf_test.rs` that proves 3MF parsing works:
   - This is a standard integration test (runs on native targets) that verifies the threemf module is available and functional.
   - Test: Create a 3MF in memory using lib3mf-core write API, parse it back with `slicecore_fileio::threemf::parse()`, assert vertex and triangle counts.
   - Test: Verify `slicecore_fileio::load_mesh()` correctly dispatches 3MF data.
   - Test: Verify that the threemf module is publicly accessible (compilation proves no cfg gate).
   - Note: We cannot run Rust tests inside a WASM runtime in this CI setup, but the WASM build step proves compilation succeeds. The integration test on native proves functional correctness.

4. Run the full workspace test suite:
   - `cargo test --workspace` -- all tests pass with no regressions
   - Verify tests from other crates that depend on slicecore-fileio still work (e.g., slicecore-engine integration tests)
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo build --target wasm32-unknown-unknown -p slicecore-fileio 2>&1 | tail -5 && cargo test -p slicecore-fileio --test wasm_3mf_test 2>&1 | tail -10</automated>
    <manual>WASM build succeeds, integration tests pass, no C dependencies in tree</manual>
  </verify>
  <done>WASM compilation succeeds for both targets, integration test proves 3MF works, no C dependencies in slicecore-fileio dependency tree</done>
</task>

<task type="auto">
  <name>Task 2: Update CI and run final verification</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
1. Check the current CI WASM build step in `.github/workflows/ci.yml`:
   - The current command is: `cargo build --target ${{ matrix.target }} --workspace --exclude slicecore-plugin --exclude slicecore-plugin-api --exclude slicecore-ai --exclude slicecore-cli`
   - slicecore-fileio was never explicitly excluded -- it was already included in the WASM build. However, the old lib3mf was cfg-gated so the WASM build would skip it silently via conditional compilation.
   - With lib3mf-core (unconditional), the WASM build now fully compiles 3MF support. No CI change needed for the build step itself.

2. Evaluate whether a WASM-specific test step should be added:
   - The existing WASM job only does `cargo build` (not `cargo test`) because running tests in WASM requires a WASM runtime.
   - Adding a WASM test runtime (wasmtime for wasm32-wasip2) could be valuable but is beyond the scope of this migration phase.
   - Decision: Do NOT add a WASM test step. The WASM build step already proves compilation succeeds, and the native integration tests prove functional correctness. This is sufficient for Phase 22.
   - If the CI WASM step needs no changes, do not modify ci.yml. Only modify if something actually needs updating.

3. Run final comprehensive verification:
   - `cargo test --workspace` -- full workspace passes
   - `cargo clippy --workspace -- -D warnings` -- no clippy warnings
   - `cargo build --target wasm32-unknown-unknown --workspace --exclude slicecore-plugin --exclude slicecore-plugin-api --exclude slicecore-ai --exclude slicecore-cli` -- WASM build passes
   - `grep -r "lib3mf[^-]" crates/slicecore-fileio/src/` returns nothing (no old lib3mf references)
   - `grep -r 'cfg(not(target_arch = "wasm32"))' crates/slicecore-fileio/src/` returns nothing (no WASM gates)

4. Verify phase success criteria from CONTEXT.md:
   - Direct adoption of lib3mf-core types and idioms (no adapter layer)
   - Full migration of both production code and test code
   - WASM 3MF parsing test proves migration unlocked WASM support
   - Behavioral equivalence in test assertions
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test --workspace 2>&1 | tail -30 && cargo build --target wasm32-unknown-unknown -p slicecore-fileio 2>&1 | tail -5</automated>
    <manual>Full workspace tests pass, WASM build succeeds, no old lib3mf references remain</manual>
  </verify>
  <done>CI verified (WASM build already includes slicecore-fileio), full workspace tests pass, WASM compilation confirmed, no C dependencies, no old lib3mf references, phase success criteria met</done>
</task>

</tasks>

<verification>
1. `cargo build --target wasm32-unknown-unknown -p slicecore-fileio` -- succeeds (3MF compiles to WASM)
2. `cargo build --target wasm32-wasip2 -p slicecore-fileio` -- succeeds
3. `cargo test --workspace` -- all tests pass, no regressions
4. `cargo clippy --workspace -- -D warnings` -- no warnings
5. `cargo tree -p slicecore-fileio | grep -i sys` -- no C library wrappers in dependency tree
6. `grep -r "lib3mf[^_-]" crates/slicecore-fileio/` -- no old lib3mf references (only lib3mf-core and lib3mf_core)
7. `grep -rn 'cfg.*wasm32' crates/slicecore-fileio/src/` -- no WASM cfg gates remain in source
</verification>

<success_criteria>
- WASM compilation succeeds for both wasm32-unknown-unknown and wasm32-wasip2
- Integration test proves 3MF parsing works end-to-end
- No C/C++ dependencies in slicecore-fileio dependency tree
- Full workspace test suite passes with no regressions
- CI WASM build step compiles slicecore-fileio with full 3MF support
- All Phase 22 success criteria from CONTEXT.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/22-migrate-from-lib3mf-to-lib3mf-core-ecosystem/22-02-SUMMARY.md`
</output>
