---
phase: 30-cli-profile-composition-and-slice-workflow
plan: 06
type: execute
wave: 3
depends_on: [30-04, 30-05]
files_modified:
  - crates/slicecore-cli/tests/cli_slice_profiles.rs
  - crates/slicecore-cli/tests/cli_output.rs
autonomous: true
requirements: [N/A-07, N/A-08, N/A-09, N/A-10, N/A-11, N/A-12]

must_haves:
  truths:
    - "E2E test: slice with -m/-f/-p produces valid G-code"
    - "E2E test: --dry-run exits 0 without writing G-code"
    - "E2E test: --save-config writes TOML file with provenance comments"
    - "E2E test: --config and -m combined exits with code 2"
    - "E2E test: missing profile exits with code 2 and helpful error"
    - "E2E test: dangerous config value exits with code 4 (without --force)"
    - "E2E test: --force allows slicing with dangerous config"
    - "E2E test: log file created alongside G-code by default"
    - "E2E test: --no-log suppresses log file"
    - "E2E test: --unsafe-defaults works without profiles"
    - "E2E test: --show-config prints annotated config"
  artifacts:
    - path: "crates/slicecore-cli/tests/cli_slice_profiles.rs"
      provides: "E2E tests for profile composition slice workflow"
      min_lines: 200
  key_links:
    - from: "crates/slicecore-cli/tests/cli_slice_profiles.rs"
      to: "crates/slicecore-cli/src/main.rs"
      via: "runs binary with assert_cmd"
      pattern: "Command::cargo_bin"
---

<objective>
Create comprehensive E2E integration tests for the entire profile composition and slice workflow.

Purpose: These tests validate the full CLI workflow end-to-end: profile resolution, composition, validation, slicing, G-code output, exit codes, log files, and all new flags. This is the final validation that everything works together.

Output: cli_slice_profiles.rs E2E test file covering all workflow scenarios.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cli-profile-composition-and-slice-workflow/30-CONTEXT.md
@.planning/phases/30-cli-profile-composition-and-slice-workflow/30-RESEARCH.md

@crates/slicecore-cli/tests/cli_output.rs
@crates/slicecore-cli/tests/cli_convert.rs

<interfaces>
From existing CLI test patterns (cli_output.rs):
```rust
use assert_cmd::Command;
use predicates::prelude::*;
use tempfile::TempDir;

fn cmd() -> Command {
    Command::cargo_bin("slicecore").unwrap()
}
```

Expected binary behavior:
- `slicecore slice input.stl -m generic_printer -f generic_pla` -> exit 0, G-code output
- `slicecore slice input.stl --config config.toml` -> exit 0 (existing path)
- `slicecore slice input.stl -m x --config y` -> exit 2 (mutual exclusion)
- `slicecore slice input.stl --dry-run -m generic_printer -f generic_pla` -> exit 0, no G-code
- `slicecore slice input.stl -m generic_printer -f generic_pla --save-config` -> exit 0, .config.toml written
- `slicecore slice input.stl --unsafe-defaults` -> exit 0 with warning
- Exit code 4 for safety validation errors (without --force)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create E2E tests for profile composition slice workflow</name>
  <files>crates/slicecore-cli/tests/cli_slice_profiles.rs</files>
  <action>
    Create `crates/slicecore-cli/tests/cli_slice_profiles.rs` with comprehensive E2E tests using assert_cmd and tempdir. Use a small test STL (the existing calibration cube or generate a minimal valid STL binary in a helper function -- check if test fixtures exist first).

    **Test categories:**

    1. **Profile composition basics:**
       - `test_slice_with_builtin_profiles`: slice with `-m generic_printer -f generic_pla` succeeds, produces G-code
       - `test_slice_with_process_override`: slice with `-m generic_printer -f generic_pla -p standard` succeeds
       - `test_slice_with_set_override`: `--set layer_height=0.1` changes layer height in output G-code
       - `test_slice_with_multiple_set`: multiple `--set` flags all apply
       - `test_slice_with_file_path_profile`: `-m /path/to/machine.toml` works (create temp TOML files)

    2. **Mutual exclusion:**
       - `test_config_and_machine_mutually_exclusive`: `--config x -m y` exits with code 2
       - `test_config_and_filament_mutually_exclusive`: `--config x -f y` exits with code 2
       - `test_config_alone_still_works`: `--config` flag alone works as before (backwards compat)

    3. **Dry run:**
       - `test_dry_run_no_gcode`: `--dry-run -m generic_printer -f generic_pla` exits 0, no G-code file created
       - `test_dry_run_shows_config_info`: stderr contains profile resolution info

    4. **Save config:**
       - `test_save_config_default_path`: `--save-config` writes {input_stem}.config.toml
       - `test_save_config_custom_path`: `--save-config /tmp/out.toml` writes to specified path
       - `test_save_config_contains_provenance`: saved file contains "Source:" comments

    5. **Show config:**
       - `test_show_config_outputs_to_stdout`: `--show-config` prints config to stderr/stdout
       - `test_show_config_with_json`: `--show-config --json` outputs valid JSON

    6. **Safety validation:**
       - `test_dangerous_config_exits_4`: create a profile with extreme values, verify exit code 4
       - `test_force_overrides_safety`: same dangerous config with `--force` exits 0

    7. **Unsafe defaults:**
       - `test_unsafe_defaults_works`: `--unsafe-defaults` with no profiles exits 0
       - `test_no_profiles_errors`: no -m/-f and no --unsafe-defaults shows error

    8. **Log file:**
       - `test_log_file_created`: default slice creates .log file alongside G-code
       - `test_no_log_suppresses`: `--no-log` prevents .log file creation
       - `test_custom_log_file`: `--log-file /tmp/custom.log` writes to specified path

    9. **Exit codes:**
       - `test_exit_0_success`: normal slice exits 0
       - `test_exit_2_profile_error`: nonexistent profile exits 2
       - `test_exit_4_safety_error`: safety validation exits 4

    10. **G-code header:**
        - `test_gcode_contains_reproduce_command`: G-code has "; Reproduce: slicecore slice ..." line
        - `test_gcode_contains_profile_checksums`: G-code has "; Profile: ..." lines
        - `test_gcode_contains_version`: G-code has "; Generated by SliceCore v..."

    Helper function: `create_test_stl(dir: &TempDir) -> PathBuf` that writes a minimal valid binary STL (single triangle) to a temp file for test use. Check existing test helpers first (e.g., calibration cube fixture) and reuse if available.
  </action>
  <verify>
    <automated>cargo test -p slicecore-cli cli_slice_profiles -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>cli_slice_profiles.rs exists with 20+ E2E tests covering profile composition, mutual exclusion, dry-run, save-config, show-config, safety validation, unsafe-defaults, log files, exit codes, and G-code header content. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-cli cli_slice_profiles` -- all E2E tests pass
- `cargo test -p slicecore-cli` -- all tests pass (existing + new)
- `cargo test -p slicecore-engine` -- all engine tests still pass
- Full test suite green: `cargo test -p slicecore-engine -p slicecore-cli`
</verification>

<success_criteria>
- 20+ E2E tests covering all workflow scenarios
- Exit codes verified: 0, 2, 4
- Mutual exclusion enforced
- --dry-run, --save-config, --show-config, --force, --unsafe-defaults, --no-log all tested
- G-code header content verified (reproduce command, checksums, version)
- Log file creation/suppression verified
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-profile-composition-and-slice-workflow/30-06-SUMMARY.md`
</output>
