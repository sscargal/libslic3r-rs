---
phase: 30-cli-profile-composition-and-slice-workflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/slicecore-engine/src/profile_resolve.rs
  - crates/slicecore-engine/src/lib.rs
  - crates/slicecore-engine/Cargo.toml
autonomous: true
requirements: [N/A-04, N/A-05]

must_haves:
  truths:
    - "Profile names are resolved to file paths by searching user dir then library index"
    - "Type-constrained search only returns profiles matching the requested type (machine/filament/process)"
    - "Exact ID match takes priority over substring match"
    - "Ambiguous queries produce an error listing all matches"
    - "File path inputs (containing / or ending .toml) bypass name resolution"
    - "User profiles shadow library profiles for the same short name"
    - "Inheritance resolution follows the inherits field to parent profiles"
  artifacts:
    - path: "crates/slicecore-engine/src/profile_resolve.rs"
      provides: "ProfileResolver with name resolution, type-constrained search, inheritance"
      exports: ["ProfileResolver", "ResolvedProfile", "ProfileSource", "ProfileError"]
  key_links:
    - from: "crates/slicecore-engine/src/profile_resolve.rs"
      to: "crates/slicecore-engine/src/profile_library.rs"
      via: "reuses load_index, ProfileIndexEntry"
      pattern: "load_index|ProfileIndexEntry"
    - from: "crates/slicecore-engine/src/profile_resolve.rs"
      to: "dirs::home_dir"
      via: "user profile directory discovery"
      pattern: "dirs::home_dir"
---

<objective>
Build the ProfileResolver: name-to-path resolution for profiles with type-constrained search, user/library prioritization, and inheritance resolution.

Purpose: The resolver is the shared lookup engine that translates user-provided profile names (e.g., "PLA_Basic", "Bambu_X1C") into concrete TOML file paths, searching user profiles first, then the library index, then filesystem scan. It replaces ad-hoc find_profiles_dir usage across CLI commands.

Output: profile_resolve.rs module with ProfileResolver, ResolvedProfile, ProfileSource, ProfileError types, and resolution/search methods.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cli-profile-composition-and-slice-workflow/30-CONTEXT.md
@.planning/phases/30-cli-profile-composition-and-slice-workflow/30-RESEARCH.md

@crates/slicecore-engine/src/profile_library.rs
@crates/slicecore-engine/src/lib.rs

<interfaces>
From crates/slicecore-engine/src/profile_library.rs:
```rust
pub struct ProfileIndexEntry {
    pub id: String,           // e.g., "orcaslicer/BBL/filament/PLA_Basic"
    pub profile_type: String, // machine/filament/process
    pub vendor: String,
    pub name: String,
    pub path: String,         // relative path within profiles dir
    // ... other fields
}

pub struct ProfileIndex {
    pub entries: Vec<ProfileIndexEntry>,
    pub profiles_dir: PathBuf,
}

pub fn load_index(profiles_dir: &Path) -> Result<ProfileIndex, EngineError>;
```

Existing find_profiles_dir in main.rs (lines 1545-1583):
```rust
fn find_profiles_dir(cli_override: Option<&Path>) -> Option<PathBuf> {
    // Search order: cli_override -> ./profiles/ -> <binary-dir>/profiles/
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ProfileResolver with name resolution and type-constrained search</name>
  <files>crates/slicecore-engine/src/profile_resolve.rs, crates/slicecore-engine/src/lib.rs, crates/slicecore-engine/Cargo.toml</files>
  <behavior>
    - Test: file path input (contains "/" or ends ".toml") returns directly without search
    - Test: exact ID match returns that profile, not substring matches
    - Test: substring match returns profile when no exact match
    - Test: case-insensitive matching ("pla" finds "PLA_Basic")
    - Test: type constraint filters results (-m only returns machine profiles)
    - Test: ambiguous query (multiple substring matches) returns error with list
    - Test: user profiles searched before library profiles
    - Test: user profile shadows library profile with same short name
    - Test: not-found error includes "did you mean?" suggestions using strsim
    - Test: wrong type usage hints correct flag (e.g., "did you mean --filament?")
    - Test: inherits field resolved through same resolution chain
    - Test: circular/self-referencing inherits detected and errored
    - Test: library directory auto-detection order ($SLICECORE_PROFILES_DIR, ./profiles/, binary-dir/profiles/, ~/.slicecore/library/)
  </behavior>
  <action>
    Add `dirs = "5"` to slicecore-engine Cargo.toml if not already present (strsim already added in Plan 01).

    Create `crates/slicecore-engine/src/profile_resolve.rs` with:

    1. **Types:**
       - `ProfileSource` enum: `User`, `Library { vendor: String }`, `BuiltIn`
       - `ResolvedProfile` struct: path (PathBuf), source (ProfileSource), profile_type (String), name (String), checksum (String -- SHA-256 of file content)
       - `ProfileError` enum using thiserror: NotFound { query, suggestions }, Ambiguous { query, matches }, TypeMismatch { query, expected_type, actual_type, hint }, CircularInheritance { chain }, IoError, ParseError

    2. **ProfileResolver** struct:
       - Fields: user_dir (Option<PathBuf>), library_dirs (Vec<PathBuf>), index (Option<ProfileIndex>)
       - `new()` constructor: takes optional profiles_dir override, discovers directories using auto-detection order: $SLICECORE_PROFILES_DIR env var -> cli --profiles-dir -> ./profiles/ -> <binary-dir>/profiles/ -> ~/.slicecore/library/. User dir is always ~/.slicecore/profiles/. Loads index.json if found in any library dir.
       - `resolve(&self, query: &str, expected_type: &str) -> Result<ResolvedProfile>`:
         1. If query contains '/' or ends with '.toml' -> treat as file path, read file, parse profile_type from TOML, validate against expected_type, compute checksum, return
         2. Search user profiles dir (~/.slicecore/profiles/{expected_type}/) for exact filename match then substring
         3. Search library index for exact ID match then case-insensitive substring on name field
         4. If single match -> return it. If multiple -> Ambiguous error with list. If none -> NotFound with strsim suggestions.
       - `resolve_inheritance(&self, profile_path: &Path) -> Result<Vec<ResolvedProfile>>`: reads TOML, checks `inherits` field, resolves parent, returns chain [parent, child]. Detects cycles via visited set. Depth limit of 5 (Claude's discretion).
       - `search(&self, query: &str, type_filter: Option<&str>, limit: usize) -> Vec<ResolvedProfile>`: for list/search commands, returns all matches.
       - `find_library_dirs() -> Vec<PathBuf>`: static method implementing the auto-detection order.

    3. Add `pub mod profile_resolve;` to lib.rs (alongside the profile_compose added in Plan 01 -- if running in parallel, both add their own mod line).

    Unit tests using tempdir for filesystem operations: create temp user and library dirs with test TOML profiles, test all resolution scenarios. Use inline TOML strings for test profiles with `profile_type` field.
  </action>
  <verify>
    <automated>cargo test -p slicecore-engine profile_resolve -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>ProfileResolver resolves names to paths with type-constrained search. Exact match priority over substring. User profiles shadow library. Ambiguous queries error with match list. Not-found suggests similar names. Type mismatch hints correct flag. Inheritance resolution with cycle detection. All unit tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-engine profile_resolve` passes all tests
- `cargo check -p slicecore-engine` compiles cleanly
- ProfileResolver correctly searches user dir before library
- Type constraints prevent cross-type resolution
</verification>

<success_criteria>
- ProfileResolver type exists with resolve(), resolve_inheritance(), search() methods
- Name-or-path auto-detection works (/ or .toml = file path)
- Case-insensitive substring matching with exact ID priority
- Ambiguous query produces error listing all matches
- Not-found error includes "did you mean?" suggestions
- Type mismatch hints correct flag
- Inheritance resolution with cycle detection
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-profile-composition-and-slice-workflow/30-02-SUMMARY.md`
</output>
