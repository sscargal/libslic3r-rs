---
phase: 31-cli-utility-commands-calibrate-and-estimate
plan: 06
type: execute
wave: 3
depends_on: ["31-02", "31-03", "31-04"]
files_modified:
  - crates/slicecore-cli/tests/cli_calibrate.rs
  - crates/slicecore-engine/tests/calibration_tests.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "All calibrate subcommands produce valid G-code"
    - "Cost estimation produces correct values for known inputs"
    - "Temperature injection places commands at correct Z heights"
    - "Dry-run and save-model work across all calibrate commands"
  artifacts:
    - path: "crates/slicecore-cli/tests/cli_calibrate.rs"
      provides: "E2E integration tests for all calibrate and estimate features"
    - path: "crates/slicecore-engine/tests/calibration_tests.rs"
      provides: "Engine-level tests for calibration mesh generation and G-code injection"
  key_links:
    - from: "crates/slicecore-cli/tests/cli_calibrate.rs"
      to: "crates/slicecore-cli/src/calibrate/mod.rs"
      via: "CLI binary invocation tests"
      pattern: "slicecore calibrate"
---

<objective>
Comprehensive E2E integration tests for all Phase 31 features: calibrate commands, cost estimation, multi-config comparison, dry-run, save-model, and output formats.

Purpose: Verify all Phase 31 features work correctly end-to-end
Output: Complete test suite covering all calibrate and estimate functionality
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-CONTEXT.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-RESEARCH.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-01-SUMMARY.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-02-SUMMARY.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-03-SUMMARY.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-04-SUMMARY.md
@.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine-level calibration tests</name>
  <files>crates/slicecore-engine/tests/calibration_tests.rs</files>
  <action>
    Create `crates/slicecore-engine/tests/calibration_tests.rs` with:

    1. **Temperature tower mesh tests:**
       - `test_temp_tower_mesh_dimensions` -- generate with known params (190-230, step 5 = 9 blocks, 8mm each), verify mesh height ~73mm (1mm base + 9*8mm), non-zero vertex count
       - `test_temp_tower_mesh_fits_bed` -- verify generated mesh passes validate_bed_fit with 220x220 bed
       - `test_temp_tower_mesh_fails_small_bed` -- verify validate_bed_fit returns Err for 50x50 bed

    2. **Temperature injection tests:**
       - `test_inject_temp_changes_correct_z` -- create mock G-code commands with Z moves at 1, 9, 17, 25, inject schedule at z=9 and z=17, verify M104/SetExtruderTemp commands appear after those Z moves
       - `test_inject_temp_preserves_all_commands` -- verify no commands are lost, output length >= input + injected count
       - `test_empty_schedule_no_changes` -- empty schedule returns commands unchanged

    3. **Cost model tests:**
       - `test_cost_model_full_inputs` -- all cost inputs provided, verify each component and total
       - `test_cost_model_partial_inputs` -- only filament price, verify others are None with hints
       - `test_cost_model_zero_expected_hours` -- verify no panic, depreciation is None
       - `test_volume_estimate_cube` -- 20mm cube (volume=8000mm3), verify filament estimate is reasonable (within order of magnitude)
       - `test_volume_estimate_disclaimer` -- verify disclaimer string is non-empty

    4. **Flow and first layer mesh tests:**
       - `test_flow_mesh_valid` -- generate flow mesh, verify non-empty
       - `test_first_layer_mesh_flat` -- generate first layer mesh for 220x220 bed, verify height < 1mm, width/depth ~80% of bed

    5. **Retraction mesh tests:**
       - `test_retraction_mesh_valid` -- generate retraction mesh, verify non-empty
  </action>
  <verify>
    <automated>cargo test -p slicecore-engine --test calibration_tests -- --nocapture</automated>
  </verify>
  <done>All engine-level calibration tests pass covering mesh generation, temp injection, cost model, and bed validation.</done>
</task>

<task type="auto">
  <name>Task 2: CLI E2E integration tests</name>
  <files>crates/slicecore-cli/tests/cli_calibrate.rs</files>
  <action>
    Extend `crates/slicecore-cli/tests/cli_calibrate.rs` (created in Plan 02, or create if not yet present) with comprehensive E2E tests. Use `std::process::Command` to invoke the built binary.

    1. **Calibrate command tests:**
       - `test_calibrate_temp_tower_generates_gcode` -- run `calibrate temp-tower --start-temp 190 --end-temp 220 --step 10 -o {tmpdir}/tower.gcode`, verify file exists, contains "M104" or "SetExtruderTemp" strings, contains temperature values
       - `test_calibrate_temp_tower_dry_run` -- run with --dry-run, verify stderr output contains model dimensions and temperature range, verify no G-code file created
       - `test_calibrate_temp_tower_save_model` -- run with --save-model {tmpdir}/tower.stl, verify STL file exists
       - `test_calibrate_temp_tower_instructions` -- verify .instructions.md file is created alongside G-code
       - `test_calibrate_retraction_generates_gcode` -- similar to temp tower
       - `test_calibrate_flow_generates_gcode` -- verify G-code contains M221 flow comments
       - `test_calibrate_first_layer_generates_gcode` -- verify single-layer G-code
       - `test_calibrate_list_shows_all` -- verify list output contains all 4 test names

    2. **Analyze-gcode cost tests:**
       - `test_analyze_gcode_cost_all_formats` -- run with --json, --csv, --markdown, verify each format produces valid output
       - `test_analyze_gcode_model_rough_estimate` -- create minimal STL, run with --model, verify rough estimate output with disclaimer

    3. **Error handling tests:**
       - `test_calibrate_bad_temp_range` -- start > end with wrong step, verify graceful error
       - `test_calibrate_invalid_output_dir` -- output to nonexistent directory, verify error message

    Use tempdir (std::env::temp_dir or tempfile crate if available) for output files. Clean up after each test.
  </action>
  <verify>
    <automated>cargo test -p slicecore-cli --test cli_calibrate -- --nocapture</automated>
  </verify>
  <done>All CLI E2E tests pass. Every calibrate subcommand produces valid output. Cost estimation, dry-run, save-model, and output formats verified.</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-engine --test calibration_tests` passes
- `cargo test -p slicecore-cli --test cli_calibrate` passes
- `cargo test --workspace` passes (no regressions)
</verification>

<success_criteria>
- 15+ integration tests covering all calibrate commands, cost estimation, dry-run, save-model
- No regressions in existing workspace tests
- Temperature injection verified at correct Z heights
- Cost model verified with known inputs
- All 4 calibrate commands produce valid G-code output
- Error cases handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/31-cli-utility-commands-calibrate-and-estimate/31-06-SUMMARY.md`
</output>
