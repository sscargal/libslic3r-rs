---
phase: 48-selective-adaptive-z-hop-control
plan: 03
type: execute
wave: 2
depends_on: ["48-01"]
files_modified:
  - crates/slicecore-engine/src/profile_import.rs
  - crates/slicecore-engine/src/profile_import_ini.rs
autonomous: true
requirements:
  - GCODE-07

must_haves:
  truths:
    - "OrcaSlicer JSON z_hop maps to z_hop.height"
    - "OrcaSlicer z_hop_types maps to z_hop.hop_type"
    - "OrcaSlicer retract_lift_enforce maps to z_hop.surface_enforce"
    - "OrcaSlicer travel_slope maps to z_hop.travel_angle"
    - "OrcaSlicer retract_lift_above maps to z_hop.above"
    - "OrcaSlicer retract_lift_below maps to z_hop.below"
    - "PrusaSlicer INI retract_lift maps to z_hop.height"
    - "Existing profile import tests still pass"
  artifacts:
    - path: "crates/slicecore-engine/src/profile_import.rs"
      provides: "JSON z-hop field mappings for OrcaSlicer/Bambu profiles"
      contains: "z_hop_types"
    - path: "crates/slicecore-engine/src/profile_import_ini.rs"
      provides: "INI z-hop field mappings for PrusaSlicer profiles"
      contains: "retract_lift"
  key_links:
    - from: "crates/slicecore-engine/src/profile_import.rs"
      to: "crates/slicecore-engine/src/config.rs"
      via: "maps upstream field names to ZHopConfig dotted paths"
      pattern: "z_hop\\."
---

<objective>
Update profile import pipelines (JSON and INI) to map OrcaSlicer/PrusaSlicer/Bambu z-hop fields to the new ZHopConfig struct paths.

Purpose: Imported profiles correctly populate all z-hop settings, not just height.
Output: Profile import with full z-hop field mapping for both JSON and INI formats.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-selective-adaptive-z-hop-control/48-CONTEXT.md
@.planning/phases/48-selective-adaptive-z-hop-control/48-RESEARCH.md
@.planning/phases/48-selective-adaptive-z-hop-control/48-01-SUMMARY.md

<interfaces>
<!-- Profile import patterns -->

From crates/slicecore-engine/src/profile_import.rs (existing pattern):
```rust
// upstream_to_config_field() maps OrcaSlicer field names to config paths:
"retraction_length" => Some("retraction.length"),
"retraction_speed" => Some("retraction.speed"),
"z_hop" => Some("retraction.z_hop"),  // OLD — needs update to "z_hop.height"

// apply_field_mapping() sets values on config:
"z_hop" => parse_and_set_f64(value, &mut config.retraction.z_hop),  // OLD — needs update
```

From crates/slicecore-engine/src/profile_import_ini.rs (existing pattern):
```rust
// prusaslicer_key_to_config_field() maps INI keys:
"retract_lift" => Some("retract_z_hop"),  // OLD — needs update

// In apply_ini_field():
"retract_lift" => {
    let first = first_comma_value(value);
    parse_and_set_f64(first, &mut config.retraction.z_hop)  // OLD — needs update
}
```

From crates/slicecore-engine/src/config.rs (Plan 01 types):
```rust
pub enum ZHopType { Normal, Slope, Spiral, Auto }
pub enum SurfaceEnforce { AllSurfaces, TopSolidAndIroning }
pub struct ZHopConfig { pub height: f64, pub hop_type: ZHopType, ... }
// config.z_hop.height, config.z_hop.hop_type, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update JSON profile import (profile_import.rs) with z-hop field mappings</name>
  <files>crates/slicecore-engine/src/profile_import.rs</files>
  <read_first>
    - crates/slicecore-engine/src/profile_import.rs (lines 545-555 for existing z_hop mapping; lines 1165-1175 for apply_field_mapping z_hop; lines 3220-3230 for test fixture with z_hop)
    - crates/slicecore-engine/src/config.rs (search for ZHopType, SurfaceEnforce, ZHopConfig — from Plan 01)
  </read_first>
  <action>
1. In `upstream_to_config_field()` function, update the existing z_hop mapping and add new ones:

**Change:**
```rust
"z_hop" => Some("retraction.z_hop"),
```
**To:**
```rust
"z_hop" => Some("z_hop.height"),
```

**Add new mappings** (add near the z_hop line):
```rust
"z_hop_types" => Some("z_hop.hop_type"),
"retract_lift_enforce" => Some("z_hop.surface_enforce"),
"travel_slope" => Some("z_hop.travel_angle"),
"retract_lift_above" => Some("z_hop.above"),
"retract_lift_below" => Some("z_hop.below"),
```

2. In `apply_field_mapping()` function, update the existing z_hop handler and add new ones:

**Change:**
```rust
"z_hop" => parse_and_set_f64(value, &mut config.retraction.z_hop),
```
**To:**
```rust
"z_hop" => parse_and_set_f64(value, &mut config.z_hop.height),
```

**Add new handlers** near the z_hop line:
```rust
"z_hop_types" => {
    // OrcaSlicer values: "Normal", "Slope", "Spiral", "Auto" (or numeric 0-3)
    match value.as_str() {
        Some("Normal") | Some("0") => { config.z_hop.hop_type = ZHopType::Normal; Ok(()) }
        Some("Slope") | Some("1") => { config.z_hop.hop_type = ZHopType::Slope; Ok(()) }
        Some("Spiral") | Some("2") => { config.z_hop.hop_type = ZHopType::Spiral; Ok(()) }
        Some("Auto") | Some("3") => { config.z_hop.hop_type = ZHopType::Auto; Ok(()) }
        _ => Ok(()) // Unknown value, ignore
    }
}
"retract_lift_enforce" => {
    // OrcaSlicer: "All Surfaces" or "Top Only" (or numeric)
    match value.as_str() {
        Some("All Surfaces") | Some("0") => { config.z_hop.surface_enforce = SurfaceEnforce::AllSurfaces; Ok(()) }
        Some("Top Only") | Some("1") => { config.z_hop.surface_enforce = SurfaceEnforce::TopSolidAndIroning; Ok(()) }
        _ => Ok(()) // Unknown, keep default
    }
}
"travel_slope" => parse_and_set_f64(value, &mut config.z_hop.travel_angle),
"retract_lift_above" => parse_and_set_f64(value, &mut config.z_hop.above),
"retract_lift_below" => parse_and_set_f64(value, &mut config.z_hop.below),
```

3. Add necessary imports at the top of profile_import.rs:
```rust
use crate::config::{ZHopType, SurfaceEnforce};
```

4. Update existing test fixture (line ~3223) that sets `z_hop` to validate the new path:
```rust
obj.insert("z_hop".into(), json!("0.3"));
```
This should now set `config.z_hop.height` to 0.3 instead of `config.retraction.z_hop`. Update the assertion to check `config.z_hop.height`.

5. Add new test `test_profile_import_z_hop_type_mapping`:
```rust
#[test]
fn test_profile_import_z_hop_type_mapping() {
    // Test z_hop_types field mapping
    assert_eq!(upstream_to_config_field("z_hop_types"), Some("z_hop.hop_type"));
    assert_eq!(upstream_to_config_field("retract_lift_enforce"), Some("z_hop.surface_enforce"));
    assert_eq!(upstream_to_config_field("travel_slope"), Some("z_hop.travel_angle"));
    assert_eq!(upstream_to_config_field("retract_lift_above"), Some("z_hop.above"));
    assert_eq!(upstream_to_config_field("retract_lift_below"), Some("z_hop.below"));
}
```

6. Add integration test that imports a full z-hop config from JSON:
```rust
#[test]
fn test_profile_import_full_z_hop_config() {
    let mut config = PrintConfig::default();
    let mut obj = serde_json::Map::new();
    obj.insert("z_hop".into(), json!("0.5"));
    obj.insert("z_hop_types".into(), json!("Spiral"));
    obj.insert("retract_lift_enforce".into(), json!("Top Only"));
    obj.insert("travel_slope".into(), json!("60"));
    obj.insert("retract_lift_above".into(), json!("0.3"));
    obj.insert("retract_lift_below".into(), json!("10.0"));

    // Apply each field
    for (key, val) in &obj {
        apply_field_mapping(key, val, &mut config).unwrap();
    }

    assert!((config.z_hop.height - 0.5).abs() < 1e-9);
    assert_eq!(config.z_hop.hop_type, ZHopType::Spiral);
    assert_eq!(config.z_hop.surface_enforce, SurfaceEnforce::TopSolidAndIroning);
    assert!((config.z_hop.travel_angle - 60.0).abs() < 1e-9);
    assert!((config.z_hop.above - 0.3).abs() < 1e-9);
    assert!((config.z_hop.below - 10.0).abs() < 1e-9);
}
```
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine profile_import 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - profile_import.rs contains `"z_hop" => Some("z_hop.height")`
    - profile_import.rs contains `"z_hop_types" => Some("z_hop.hop_type")`
    - profile_import.rs contains `"retract_lift_enforce" => Some("z_hop.surface_enforce")`
    - profile_import.rs contains `"travel_slope" => Some("z_hop.travel_angle")`
    - profile_import.rs contains `"retract_lift_above" => Some("z_hop.above")`
    - profile_import.rs contains `"retract_lift_below" => Some("z_hop.below")`
    - profile_import.rs does NOT contain `config.retraction.z_hop`
    - `cargo test -p slicecore-engine profile_import` passes
  </acceptance_criteria>
  <done>JSON profile import maps all 6 OrcaSlicer z-hop fields to ZHopConfig paths. Existing tests updated, new tests added for z-hop type and surface enforce mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Update INI profile import (profile_import_ini.rs) with z-hop field mappings</name>
  <files>crates/slicecore-engine/src/profile_import_ini.rs</files>
  <read_first>
    - crates/slicecore-engine/src/profile_import_ini.rs (lines 340-345 for retract_lift key mapping; lines 958-963 for retract_lift apply handler; lines 2120-2126 for retract_lift test)
    - crates/slicecore-engine/src/config.rs (search for ZHopType, SurfaceEnforce — from Plan 01)
  </read_first>
  <action>
1. In `prusaslicer_key_to_config_field()` function, update retract_lift mapping:

**Change:**
```rust
"retract_lift" => Some("retract_z_hop"),
```
**To:**
```rust
"retract_lift" => Some("z_hop.height"),
```

**Add new mappings** nearby:
```rust
"retract_lift_above" => Some("z_hop.above"),
"retract_lift_below" => Some("z_hop.below"),
```

2. In `apply_ini_field()` function, update the retract_lift handler:

**Change:**
```rust
"retract_lift" => {
    let first = first_comma_value(value);
    parse_and_set_f64(first, &mut config.retraction.z_hop)
}
```
**To:**
```rust
"retract_lift" => {
    let first = first_comma_value(value);
    parse_and_set_f64(first, &mut config.z_hop.height)
}
```

**Add new handlers:**
```rust
"retract_lift_above" => {
    let first = first_comma_value(value);
    parse_and_set_f64(first, &mut config.z_hop.above)
}
"retract_lift_below" => {
    let first = first_comma_value(value);
    parse_and_set_f64(first, &mut config.z_hop.below)
}
```

3. Update the test assertion (line ~2124):

**Change:**
```rust
assert_eq!(prusaslicer_key_to_config_field("retract_lift"), Some("retract_z_hop"));
```
**To:**
```rust
assert_eq!(prusaslicer_key_to_config_field("retract_lift"), Some("z_hop.height"));
```

4. Add test for new INI mappings:
```rust
#[test]
fn test_ini_z_hop_field_mappings() {
    assert_eq!(prusaslicer_key_to_config_field("retract_lift"), Some("z_hop.height"));
    assert_eq!(prusaslicer_key_to_config_field("retract_lift_above"), Some("z_hop.above"));
    assert_eq!(prusaslicer_key_to_config_field("retract_lift_below"), Some("z_hop.below"));
}
```

5. Add integration test for INI z-hop import:
```rust
#[test]
fn test_ini_retract_lift_sets_z_hop_height() {
    let mut config = PrintConfig::default();
    apply_ini_field("retract_lift", "0.6", &mut config).unwrap();
    assert!((config.z_hop.height - 0.6).abs() < 1e-9);
}
```

6. Search for any other references to `config.retraction.z_hop` in profile_import_ini.rs and update them to `config.z_hop.height`.
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine profile_import_ini 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - profile_import_ini.rs contains `"retract_lift" => Some("z_hop.height")`
    - profile_import_ini.rs contains `"retract_lift_above" => Some("z_hop.above")`
    - profile_import_ini.rs contains `"retract_lift_below" => Some("z_hop.below")`
    - profile_import_ini.rs does NOT contain `config.retraction.z_hop`
    - profile_import_ini.rs does NOT contain `Some("retract_z_hop")`
    - `cargo test -p slicecore-engine profile_import_ini` passes
  </acceptance_criteria>
  <done>INI profile import maps retract_lift to z_hop.height, adds retract_lift_above/below mappings. All INI import tests pass.</done>
</task>

</tasks>

<verification>
```bash
# All profile import tests pass
cargo test -p slicecore-engine profile_import

# No old z_hop paths remain
grep -rn "retraction\.z_hop\|retract_z_hop" crates/slicecore-engine/src/profile_import.rs crates/slicecore-engine/src/profile_import_ini.rs

# Full workspace compiles and tests pass
cargo test -p slicecore-engine --lib
```
</verification>

<success_criteria>
- JSON profile import maps all 6 OrcaSlicer z-hop fields
- INI profile import maps retract_lift + retract_lift_above/below
- No references to old retraction.z_hop path remain in import files
- All existing and new profile import tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/48-selective-adaptive-z-hop-control/48-03-SUMMARY.md`
</output>
