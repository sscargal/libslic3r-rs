---
phase: 27-build-plate-auto-arrangement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/slicecore-engine/src/config.rs
  - crates/slicecore-engine/src/profile_import.rs
  - crates/slicecore-engine/src/profile_import_ini.rs
autonomous: true
requirements: [ADV-02]
must_haves:
  truths:
    - "SequentialConfig has gantry_width, gantry_depth, and extruder_clearance_polygon fields"
    - "MachineConfig has extruder_count field for multi-head detection"
    - "New config fields have sensible defaults and deserialize from TOML"
    - "Profile import maps gantry/clearance fields from upstream slicer formats"
  artifacts:
    - path: "crates/slicecore-engine/src/config.rs"
      provides: "Expanded SequentialConfig with gantry zone fields"
      contains: "gantry_width"
    - path: "crates/slicecore-engine/src/profile_import.rs"
      provides: "JSON profile mapping for new fields"
    - path: "crates/slicecore-engine/src/profile_import_ini.rs"
      provides: "INI profile mapping for new fields"
  key_links:
    - from: "crates/slicecore-engine/src/config.rs"
      to: "crates/slicecore-arrange/src/config.rs"
      via: "SequentialConfig fields consumed by arrange crate"
      pattern: "gantry_width|gantry_depth|extruder_clearance_polygon"
---

<objective>
Expand PrintConfig with gantry clearance and multi-head detection fields needed by the arrangement algorithm.

Purpose: The arrange crate needs gantry zone dimensions and extruder count from PrintConfig. Adding these fields now (Wave 1) allows Plan 03 to consume them without blocking.
Output: Updated SequentialConfig with gantry_width, gantry_depth, extruder_clearance_polygon; MachineConfig with extruder_count; profile import mappings updated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-build-plate-auto-arrangement/27-CONTEXT.md

@crates/slicecore-engine/src/config.rs
@crates/slicecore-engine/src/profile_import.rs
@crates/slicecore-engine/src/profile_import_ini.rs

<interfaces>
From config.rs SequentialConfig (current):
```rust
pub struct SequentialConfig {
    pub enabled: bool,
    pub extruder_clearance_radius: f64,
    pub extruder_clearance_height: f64,
}
```

From config.rs MachineConfig (current):
```rust
pub struct MachineConfig {
    pub nozzle_diameters: Vec<f64>,
    // ... other fields
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand SequentialConfig and MachineConfig with gantry fields</name>
  <files>crates/slicecore-engine/src/config.rs</files>
  <action>
    1. Add fields to `SequentialConfig`:
       - `gantry_width: f64` -- Width of gantry/carriage in mm (X direction). Default 0.0 (means use cylinder model with extruder_clearance_radius).
       - `gantry_depth: f64` -- Depth of gantry/carriage in mm (Y direction). Default 0.0.
       - `extruder_clearance_polygon: Vec<(f64, f64)>` -- Custom polygon for clearance zone, points in mm relative to nozzle center. Default empty vec (means use cylinder or rectangle model).
       - Update Default impl to include new fields with defaults.
       - Add doc comments explaining the three gantry models: if extruder_clearance_polygon non-empty, use custom polygon; else if gantry_width > 0, use rectangular; else use cylinder with extruder_clearance_radius.

    2. Add field to `MachineConfig`:
       - `extruder_count: u32` -- Number of extruders/toolheads. Default 1. Used to auto-detect multi-head printers for material grouping decisions.
       - Update Default impl.

    3. Ensure all new fields have `serde(default)` behavior (the structs already use `#[serde(default)]` at struct level, so this is automatic).

    4. Run existing tests to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine -- config && cargo clippy -p slicecore-engine --all-features -- -D warnings</automated>
  </verify>
  <done>SequentialConfig has gantry_width, gantry_depth, extruder_clearance_polygon fields; MachineConfig has extruder_count; all existing tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Map new fields in profile importers</name>
  <files>
    crates/slicecore-engine/src/profile_import.rs,
    crates/slicecore-engine/src/profile_import_ini.rs
  </files>
  <action>
    1. In `profile_import.rs` (JSON/OrcaSlicer mapper):
       - Map "extruder_clearance_radius" -> config.sequential.extruder_clearance_radius
       - Map "extruder_clearance_max_radius" -> config.sequential.extruder_clearance_radius (OrcaSlicer naming)
       - Map "extruder_clearance_height_to_rod" / "extruder_clearance_height_to_lid" -> config.sequential.extruder_clearance_height (take max of both)
       - Map "gantry_width" -> config.sequential.gantry_width (if present)
       - These upstream fields may not exist in most profiles, so only apply if present (match arm in apply_field_mapping or passthrough).

    2. In `profile_import_ini.rs` (PrusaSlicer INI mapper):
       - Map "extruder_clearance_radius" -> config.sequential.extruder_clearance_radius
       - Map "extruder_clearance_height" -> config.sequential.extruder_clearance_height
       - These already exist in some PrusaSlicer profiles. Route to sequential sub-config instead of passthrough.

    3. Both importers: the `extruder_count` field is not typically in upstream profiles (it's derived from nozzle_diameters length), so no mapping needed. Add a helper method `MachineConfig::effective_extruder_count(&self) -> u32` that returns max(extruder_count, nozzle_diameters.len()) to handle both explicit config and inferred count.

    4. Run existing profile import tests to confirm no regressions.
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine -- profile_import && cargo clippy -p slicecore-engine --all-features -- -D warnings</automated>
  </verify>
  <done>Profile importers route gantry/clearance fields to SequentialConfig; effective_extruder_count helper works; all existing tests pass</done>
</task>

</tasks>

<verification>
```bash
cargo fmt --all -- --check
cargo clippy -p slicecore-engine --all-features -- -D warnings
cargo test -p slicecore-engine --all-features
RUSTDOCFLAGS="-D warnings" cargo doc -p slicecore-engine --no-deps --all-features
```
</verification>

<success_criteria>
- SequentialConfig has gantry_width, gantry_depth, extruder_clearance_polygon with serde defaults
- MachineConfig has extruder_count with effective_extruder_count() helper
- Profile import handles clearance/gantry fields from OrcaSlicer and PrusaSlicer formats
- All existing engine tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/27-build-plate-auto-arrangement/27-02-SUMMARY.md`
</output>
