---
phase: 34-support-config-and-advanced-feature-profile-import-mapping
plan: 06
type: execute
wave: 4
depends_on: [34-02, 34-03, 34-04, 34-05]
files_modified:
  - crates/slicecore-engine/tests/phase34_integration.rs
  - crates/slicecore-engine/src/profile_import.rs
  - designDocs/MAPPING_COVERAGE_REPORT.md
  - designDocs/CONFIG_PARITY_AUDIT.md
autonomous: true
requirements: [PASSTHROUGH-THRESHOLD, ROUND-TRIP, RECONVERT]

must_haves:
  truths:
    - "Integration tests verify support-heavy, bridge-heavy, and tree-support profiles import correctly"
    - "Passthrough threshold test asserts <5% of upstream keys in passthrough for representative profiles"
    - "Coverage report shows mapping improvement numbers"
    - "Re-conversion of profiles completes without regression"
    - "CONFIG_PARITY_AUDIT.md Section 4 updated with final coverage numbers"
  artifacts:
    - path: "crates/slicecore-engine/tests/phase34_integration.rs"
      provides: "Integration tests for all Phase 34 mappings"
      contains: "fn test_support_profile_import"
    - path: "designDocs/MAPPING_COVERAGE_REPORT.md"
      provides: "Coverage improvement report"
      contains: "## Coverage Summary"
  key_links:
    - from: "crates/slicecore-engine/tests/phase34_integration.rs"
      to: "crates/slicecore-engine/src/profile_import.rs"
      via: "import_orcaslicer_profile function calls"
      pattern: "import_orcaslicer_profile"
---

<objective>
Integration tests, passthrough threshold assertion, re-conversion of profile library, coverage reporting, and CONFIG_PARITY_AUDIT.md update.

Purpose: Verify all Phase 34 mappings work end-to-end, assert quality via passthrough threshold, document coverage improvement, and update the audit with final numbers.
Output: Integration test file, coverage report, updated audit doc, re-converted profiles.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-CONTEXT.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-RESEARCH.md
@designDocs/PHASE34_FIELD_INVENTORY.md

@.planning/phases/33-p1-config-gap-closure-profile-fidelity-fields/33-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 34 integration tests with passthrough threshold assertion</name>
  <files>crates/slicecore-engine/tests/phase34_integration.rs</files>
  <read_first>
    - crates/slicecore-engine/tests/phase33_p1_integration.rs
    - crates/slicecore-engine/src/profile_import.rs
    - crates/slicecore-engine/src/support/config.rs
    - crates/slicecore-engine/src/config.rs
    - crates/slicecore-engine/src/custom_gcode.rs
    - crates/slicecore-engine/src/gcode_template.rs
  </read_first>
  <action>
Create `crates/slicecore-engine/tests/phase34_integration.rs` with these test groups:

**1. Support config import tests:**
- `test_support_profile_import_json`: Construct a `serde_json::json!` object with all support fields (enable_support: "1", support_type: "tree", support_threshold_angle: "45", support_top_z_distance: "0.2", support_object_xy_distance: "0.5", support_interface_top_layers: "3", support_base_pattern: "rectilinear", etc.). Import via `import_orcaslicer_profile`. Assert each field was set correctly on config.support.
- `test_support_type_mapping`: Test all SupportType variants: "none"->None, "normal"->Traditional, "tree"->Tree, "default"->Auto. Test both OrcaSlicer and PrusaSlicer vocabulary.
- `test_bridge_config_import`: JSON with bridge_speed, bridge_flow_ratio, bridge_fan_speed, bridge_acceleration. Assert config.support.bridge.* values.
- `test_tree_support_import`: JSON with tree_support_branch_angle, tree_support_branch_diameter. Assert config.support.tree.* values.
- `test_support_density_from_spacing`: JSON with support_base_pattern_spacing: "2.0". Assert config.support.support_density is approximately line_width/2.0 (around 0.2).

**2. ScarfJoint import tests:**
- `test_scarf_joint_import_json`: JSON with seam_slope_type, seam_slope_conditional, seam_slope_start_height, seam_slope_steps, etc. Assert config.scarf_joint.* values.

**3. MultiMaterial import tests:**
- `test_multi_material_import_json`: JSON with wipe_tower, wipe_tower_x, wipe_tower_y, wipe_tower_width. Assert config.multi_material.* values.

**4. CustomGcode import tests:**
- `test_custom_gcode_hooks_import`: JSON with before_layer_change_gcode, toolchange_gcode. Assert config.custom_gcode.* values including _original fields.

**5. P2 niche field tests:**
- `test_p2_fields_import`: JSON with slicing_tolerance, thumbnails, silent_mode, nozzle_hrc, timelapse_type. Assert typed field values.
- `test_p2_fields_toml_roundtrip`: Create config with P2 values, serialize to TOML, deserialize back, assert values match.

**6. G-code template translation tests:**
- `test_gcode_template_translation_orcaslicer`: Call translate_gcode_template with OrcaSlicer variable syntax. Assert variables are replaced.
- `test_gcode_template_no_double_replacement`: Template with "{layer_z}" should not get mangled by partial "{layer}" replacement.

**7. Passthrough threshold test:**
- `test_passthrough_threshold`: Load a representative real OrcaSlicer profile JSON from profiles/ directory (pick one with many fields). Import it. Count passthrough keys vs total keys. Assert passthrough ratio < 0.05 (5%).

**8. Range validation tests:**
- `test_support_range_validation`: Create config with out-of-range support values. Run validation. Assert warnings are generated.

Use `serde_json::json!` macro for test data construction (consistent with phase33 patterns).
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine --test phase34_integration 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File crates/slicecore-engine/tests/phase34_integration.rs exists
    - File contains `fn test_support_profile_import_json`
    - File contains `fn test_support_type_mapping`
    - File contains `fn test_scarf_joint_import_json`
    - File contains `fn test_multi_material_import_json`
    - File contains `fn test_custom_gcode_hooks_import`
    - File contains `fn test_p2_fields_import`
    - File contains `fn test_gcode_template_translation`
    - File contains `fn test_passthrough_threshold`
    - cargo test -p slicecore-engine --test phase34_integration passes
  </acceptance_criteria>
  <done>Integration tests pass for all Phase 34 field mappings including passthrough threshold and G-code translation</done>
</task>

<task type="auto">
  <name>Task 2: Re-convert profiles and generate coverage report</name>
  <files>designDocs/MAPPING_COVERAGE_REPORT.md, designDocs/CONFIG_PARITY_AUDIT.md</files>
  <read_first>
    - designDocs/CONFIG_PARITY_AUDIT.md
    - designDocs/PHASE34_FIELD_INVENTORY.md
  </read_first>
  <action>
**1. Run profile re-conversion** using the existing convert-profile CLI:
```bash
# Re-convert a sample of upstream profiles to test the new mappings
# Use the same pattern as Phase 32/33 re-conversion
find profiles/ -name "*.json" -type f | head -50 | while read f; do
  cargo run --release -- convert-profile "$f" --output /dev/null 2>&1
done
```
If the project has a batch conversion script or command, use that instead. The goal is to verify the new mappings don't break existing profile conversion.

**2. Generate coverage report** by comparing before/after passthrough contents:
- Pick 5 representative profiles (one with heavy support settings, one with scarf joint, one with multi-material, one general, one PrusaSlicer INI)
- Import each, count: total_upstream_keys, mapped_to_typed, in_passthrough
- Calculate improvement from Phase 33 baseline

**3. Create `designDocs/MAPPING_COVERAGE_REPORT.md`** with:
```markdown
# Mapping Coverage Report — Phase 34

## Coverage Summary
| Metric | Before Phase 34 | After Phase 34 | Change |
|--------|:---:|:---:|:---:|
| Typed fields | N | M | +K |
| Mapped upstream keys | N | M | +K |
| Passthrough ratio (representative profile) | X% | Y% | -Z% |

## Section Coverage
| Sub-struct | Before | After |
|------------|:---:|:---:|
| SupportConfig + subs | 0% | 100% |
| ScarfJointConfig | 0% | 100% |
| MultiMaterialConfig | 0% | 100% |
| CustomGcodeHooks | 0% | 100% |
| PostProcessConfig + subs | 0% | ~80% |
| P2 niche fields | 0% | 100% |

## Per-Profile Results
[Table with 5 representative profiles and their coverage numbers]
```

**4. Update `designDocs/CONFIG_PARITY_AUDIT.md` Section 4** (lines around 454-479):
- Update the coverage table with new percentages for all sub-structs
- Update overall typed field count
- Update overall mapping coverage percentage
- Note: SupportConfig goes from 0% to ~100%, ScarfJoint 0% to ~100%, etc.

**5. Verify no regressions:** Run the full test suite.
```bash
cargo test -p slicecore-engine
```
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - designDocs/MAPPING_COVERAGE_REPORT.md exists
    - MAPPING_COVERAGE_REPORT.md contains "## Coverage Summary"
    - MAPPING_COVERAGE_REPORT.md contains "SupportConfig" with non-0% coverage
    - CONFIG_PARITY_AUDIT.md Section 4 table shows updated percentages (SupportConfig no longer 0%)
    - cargo test -p slicecore-engine passes (full suite, not just --lib)
  </acceptance_criteria>
  <done>Profiles re-converted without regression, coverage report generated, CONFIG_PARITY_AUDIT.md updated with final Phase 34 numbers</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-engine` full suite passes
- Coverage report shows significant improvement from 0% to near-100% for 5 previously unmapped sections
- CONFIG_PARITY_AUDIT.md reflects current state
- Passthrough threshold test passes (<5%)
</verification>

<success_criteria>
All Phase 34 mappings verified end-to-end via integration tests. Passthrough below 5% threshold on representative profiles. Coverage report committed. Audit doc updated. No regressions in existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-06-SUMMARY.md`
</output>
