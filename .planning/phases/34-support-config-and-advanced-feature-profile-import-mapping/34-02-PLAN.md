---
phase: 34-support-config-and-advanced-feature-profile-import-mapping
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - crates/slicecore-engine/src/profile_import.rs
  - crates/slicecore-engine/src/profile_import_ini.rs
  - crates/slicecore-engine/src/support/config.rs
autonomous: true
requirements: [SUPPORT-MAP]

must_haves:
  truths:
    - "OrcaSlicer JSON profiles with support fields import into typed SupportConfig/BridgeConfig/TreeSupportConfig fields"
    - "PrusaSlicer INI profiles with support_material fields import into typed SupportConfig fields"
    - "SupportType enum correctly maps from both slicers' vocabularies"
    - "Spacing-to-density conversion produces reasonable values"
  artifacts:
    - path: "crates/slicecore-engine/src/profile_import.rs"
      provides: "Support config JSON field mapping match arms"
      contains: "support_threshold_angle"
    - path: "crates/slicecore-engine/src/profile_import_ini.rs"
      provides: "Support config INI field mapping match arms"
      contains: "support_material_threshold"
  key_links:
    - from: "crates/slicecore-engine/src/profile_import.rs"
      to: "crates/slicecore-engine/src/support/config.rs"
      via: "config.support.* field assignments in match arms"
      pattern: "config\\.support\\."
---

<objective>
Map all ~27 SupportConfig + BridgeConfig + TreeSupportConfig fields from both OrcaSlicer JSON and PrusaSlicer INI profiles into typed fields.

Purpose: SupportConfig is at 0% upstream mapping despite having fully defined internal types. This is the largest unmapped section.
Output: Complete support field mapping in both profile_import.rs and profile_import_ini.rs with enum mapping helpers.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-CONTEXT.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-RESEARCH.md
@designDocs/PHASE34_FIELD_INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add support enum mappers and OrcaSlicer JSON support field mappings</name>
  <files>crates/slicecore-engine/src/profile_import.rs, crates/slicecore-engine/src/support/config.rs</files>
  <read_first>
    - crates/slicecore-engine/src/profile_import.rs
    - crates/slicecore-engine/src/support/config.rs
    - crates/slicecore-engine/src/config.rs
    - designDocs/PHASE34_FIELD_INVENTORY.md
  </read_first>
  <action>
1. **Add new enums to support/config.rs if not already present** (check first):
   - `SupportPattern` enum: `Default`, `Rectilinear`, `Grid`, `Honeycomb`, `Lightning` (with serde rename for lowercase string mapping)
   - `InterfacePattern` enum: `Default`, `Rectilinear`, `Grid`, `Concentric` (with serde rename)
   - If SupportType already exists with variants (None, Auto, Traditional, Tree), verify it's adequate. Add any missing variants discovered in the inventory.

2. **Add pub(crate) enum mapper functions** in profile_import.rs (following the map_brim_type pattern from Phase 33):
   - `map_support_type(value: &str) -> Option<SupportType>`: Map OrcaSlicer values ("none"/"disable" -> None, "normal"/"grid" -> Traditional, "tree"/"tree(auto)"/"tree_slim"/"organic" -> Tree, "default"/"auto" -> Auto) AND PrusaSlicer values ("0" -> None, "1" -> Auto)
   - `map_support_pattern(value: &str) -> Option<SupportPattern>`: Map "default"/"rectilinear"/"grid"/"honeycomb"/"lightning"
   - `map_interface_pattern(value: &str) -> Option<InterfacePattern>`: Map "default"/"rectilinear"/"grid"/"concentric"/"auto"

3. **Add OrcaSlicer JSON match arms** in `apply_field_mapping` for ALL support fields from the inventory. Key mappings include:
   - `"enable_support" | "support_material"` -> `config.support.enabled` (bool)
   - `"support_type" | "support_material_type"` -> `config.support.support_type` via map_support_type
   - `"support_threshold_angle" | "support_material_threshold"` -> `config.support.overhang_angle` (f64)
   - `"support_base_pattern" | "support_material_pattern"` -> `config.support.support_pattern` via map_support_pattern
   - `"support_on_build_plate_only" | "support_material_buildplate_only"` -> `config.support.build_plate_only` (bool)
   - `"support_top_z_distance" | "support_material_contact_distance"` -> `config.support.z_gap` (f64)
   - `"support_object_xy_distance" | "support_material_xy_spacing"` -> `config.support.xy_gap` (f64)
   - `"support_interface_top_layers" | "support_material_interface_layers"` -> `config.support.interface_layers` (u32)
   - `"support_interface_bottom_layers" | "support_material_bottom_interface_layers"` -> `config.support.bottom_interface_layers` (u32)
   - `"support_interface_pattern" | "support_material_interface_pattern"` -> `config.support.interface_pattern` via map_interface_pattern
   - `"support_base_pattern_spacing"` -> density conversion: `config.support.support_density = (line_width / spacing).clamp(0.0, 1.0)` where line_width defaults to 0.4 if not yet set
   - `"support_interface_spacing"` -> similar density conversion for `config.support.interface_density`
   - `"support_expansion"` -> `config.support.expand_distance` (f64)
   - `"raft_layers"` -> `config.support.raft_layers` (u32)
   - `"raft_first_layer_expansion"` -> `config.support.raft_expansion` (f64, if field exists)
   - Bridge config fields: `"bridge_speed"` -> `config.support.bridge.speed` (f64), `"bridge_flow"` | `"bridge_flow_ratio"` -> `config.support.bridge.flow_ratio`, `"bridge_fan_speed"` -> `config.support.bridge.fan_speed`, `"bridge_acceleration"` -> `config.support.bridge.acceleration`
   - Tree support fields: `"tree_support_branch_angle"` | `"support_tree_angle"` -> `config.support.tree.branch_angle`, `"tree_support_branch_diameter"` | `"support_tree_branch_diameter"` -> `config.support.tree.branch_diameter`, `"tree_support_branch_distance"` | `"support_tree_top_rate"` -> `config.support.tree.branch_distance`
   - Any additional support fields found in the inventory

4. **Add upstream_key_to_config_field entries** for all new support keys (the documentation HashMap).

5. **Verify compilation:** `cargo check -p slicecore-engine`
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo check -p slicecore-engine 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - profile_import.rs contains `fn map_support_type` function
    - profile_import.rs contains `fn map_support_pattern` function
    - profile_import.rs contains match arm for "enable_support"
    - profile_import.rs contains match arm for "support_threshold_angle"
    - profile_import.rs contains match arm for "support_top_z_distance"
    - profile_import.rs contains match arm for "bridge_speed"
    - profile_import.rs contains match arm for "tree_support_branch_angle"
    - profile_import.rs contains at least 15 new support-related match arms
    - cargo check -p slicecore-engine succeeds
  </acceptance_criteria>
  <done>All support config fields mapped from OrcaSlicer JSON with enum mappers, density conversion, and bridge/tree sub-struct mapping</done>
</task>

<task type="auto">
  <name>Task 2: Add PrusaSlicer INI support field mappings</name>
  <files>crates/slicecore-engine/src/profile_import_ini.rs</files>
  <read_first>
    - crates/slicecore-engine/src/profile_import_ini.rs
    - crates/slicecore-engine/src/profile_import.rs
    - designDocs/PHASE34_FIELD_INVENTORY.md
  </read_first>
  <action>
1. **Add PrusaSlicer INI match arms** in `apply_prusaslicer_field_mapping` for all support fields with PrusaSlicer equivalents. PrusaSlicer uses `support_material_*` prefix:
   - `"support_material"` -> `config.support.enabled` (bool, "1"/"0")
   - `"support_material_auto"` -> `config.support.support_type` (if "1", set Auto)
   - `"support_material_threshold"` -> `config.support.overhang_angle` (f64)
   - `"support_material_pattern"` -> `config.support.support_pattern` via map_support_pattern (import the pub(crate) function)
   - `"support_material_buildplate_only"` -> `config.support.build_plate_only` (bool)
   - `"support_material_contact_distance"` -> `config.support.z_gap` (f64)
   - `"support_material_xy_spacing"` -> `config.support.xy_gap` (f64)
   - `"support_material_interface_layers"` -> `config.support.interface_layers` (u32)
   - `"support_material_bottom_interface_layers"` -> `config.support.bottom_interface_layers` (u32)
   - `"support_material_interface_pattern"` -> `config.support.interface_pattern` via map_interface_pattern
   - `"support_material_interface_spacing"` -> density conversion for `config.support.interface_density`
   - `"support_material_spacing"` -> density conversion for `config.support.support_density`
   - `"support_material_with_sheath"` -> bool field if exists
   - `"raft_layers"` -> `config.support.raft_layers` (u32)
   - Bridge fields: `"bridge_speed"` -> `config.support.bridge.speed`, `"bridge_flow_ratio"` -> `config.support.bridge.flow_ratio`, `"bridge_fan_speed"` -> `config.support.bridge.fan_speed`, `"bridge_acceleration"` -> `config.support.bridge.acceleration`

2. **Import shared enum mapper functions** from profile_import.rs: `map_support_type`, `map_support_pattern`, `map_interface_pattern` (they should be `pub(crate)`).

3. **Add prusaslicer_key_to_config_field entries** for all new support keys.

4. **Verify compilation and run existing tests:**
   - `cargo check -p slicecore-engine`
   - `cargo test -p slicecore-engine --lib`
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - profile_import_ini.rs contains match arm for "support_material"
    - profile_import_ini.rs contains match arm for "support_material_threshold"
    - profile_import_ini.rs contains match arm for "support_material_contact_distance"
    - profile_import_ini.rs contains match arm for "bridge_speed"
    - profile_import_ini.rs imports map_support_type or map_support_pattern from profile_import
    - profile_import_ini.rs contains at least 10 new support-related match arms
    - cargo test -p slicecore-engine --lib passes
  </acceptance_criteria>
  <done>PrusaSlicer INI support field mappings complete, sharing enum mappers with JSON importer, all existing tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-engine --lib` passes
- `grep -c "config.support\." crates/slicecore-engine/src/profile_import.rs` shows significant increase
- Both importers handle support type, angle, pattern, z_gap, xy_gap, interface layers, bridge params
</verification>

<success_criteria>
All ~27 SupportConfig fields plus BridgeConfig and TreeSupportConfig fields have upstream mapping from both OrcaSlicer JSON and PrusaSlicer INI. Enum mappers handle vocabulary differences between slicers.
</success_criteria>

<output>
After completion, create `.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-02-SUMMARY.md`
</output>
