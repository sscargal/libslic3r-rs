---
phase: 34-support-config-and-advanced-feature-profile-import-mapping
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - designDocs/PHASE34_FIELD_INVENTORY.md
autonomous: true
requirements: [SUPPORT-MAP, SCARF-MAP, MULTI-MAP, GCODE-MAP, POST-MAP, P2-FIELDS]

must_haves:
  truths:
    - "A definitive field inventory exists listing every upstream key that needs mapping"
    - "All distinct support_type values from real profiles are catalogued"
    - "All G-code template variables from real profiles are catalogued"
    - "Passthrough BTreeMap contents from current import are analyzed for promotable keys"
  artifacts:
    - path: "designDocs/PHASE34_FIELD_INVENTORY.md"
      provides: "Complete field inventory for Phase 34 implementation"
      contains: "## Support Config Fields"
  key_links: []
---

<objective>
Comprehensive field inventory scanning real upstream profiles and current codebase to produce a definitive "map these" field list for all Phase 34 work.

Purpose: The audit-first approach (per user decision) ensures no upstream keys are missed before implementation begins. Real profile scanning catches keys not in the audit doc.
Output: designDocs/PHASE34_FIELD_INVENTORY.md with categorized field lists ready for implementation in Plans 02-06.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-CONTEXT.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-RESEARCH.md
@designDocs/CONFIG_PARITY_AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scan upstream profiles and codebase for unmapped keys</name>
  <files>designDocs/PHASE34_FIELD_INVENTORY.md</files>
  <read_first>
    - designDocs/CONFIG_PARITY_AUDIT.md
    - crates/slicecore-engine/src/profile_import.rs
    - crates/slicecore-engine/src/profile_import_ini.rs
    - crates/slicecore-engine/src/config.rs
    - crates/slicecore-engine/src/support/config.rs
    - crates/slicecore-engine/src/custom_gcode.rs
  </read_first>
  <action>
Produce designDocs/PHASE34_FIELD_INVENTORY.md by performing these scans:

1. **Extract all currently-mapped upstream keys** from profile_import.rs and profile_import_ini.rs:
   - Grep all match arm string literals in apply_field_mapping and apply_array_field_mapping
   - Grep all match arm string literals in apply_prusaslicer_field_mapping
   - Also grep upstream_key_to_config_field and prusaslicer_key_to_config_field for the documentation maps

2. **Extract all support-related keys** from real upstream profiles:
   - Scan /home/steve/slicer-analysis/ directories for OrcaSlicer JSON profiles containing "support" keys
   - Scan /home/steve/slicer-analysis/ directories for PrusaSlicer INI profiles containing "support_material" keys
   - Use: `grep -roh '"support[_a-z]*"' /home/steve/slicer-analysis/ | sort -u | head -100`
   - Use: `grep -roh 'support_material[_a-z]*' /home/steve/slicer-analysis/ | sort -u | head -100`

3. **Extract all scarf/seam_slope keys** from real profiles:
   - `grep -roh '"seam_slope[_a-z]*"' /home/steve/slicer-analysis/ | sort -u`

4. **Extract all multi-material / wipe_tower keys**:
   - `grep -roh '"wipe_tower[_a-z]*"\|"prime_[_a-z]*"\|"purge_[_a-z]*"' /home/steve/slicer-analysis/ | sort -u`

5. **Extract all custom G-code hook keys**:
   - `grep -roh '"[a-z_]*gcode[_a-z]*"' /home/steve/slicer-analysis/ | sort -u | head -50`

6. **Extract all G-code template variables** from profile G-code fields:
   - `grep -roh '{[a-z_]*}' /home/steve/slicer-analysis/ | sort -u | head -100`

7. **Check passthrough BTreeMap** for promotable keys by running a quick import of a representative profile and examining what ends up in passthrough:
   - Use the convert-profile CLI or write a small test

8. **Cross-reference** discovered keys against currently-mapped keys to produce the gap list.

9. **Enumerate all P2 niche fields** from CONFIG_PARITY_AUDIT.md Section 3 (lines 425-451) and verify each has/lacks a typed field.

10. **Check straggler fields** in partially-mapped sections:
    - IroningConfig: check ironing_angle mapping
    - SequentialConfig: check unmapped fields
    - Any other section at <100% in audit Section 4

Output the inventory as a markdown document with these sections:
- ## Support Config Fields (OrcaSlicer key | PrusaSlicer key | Our field path | Status: new-mapping/existing/needs-new-field)
- ## Scarf Joint Fields
- ## Multi-Material Fields
- ## Custom G-code Hook Fields
- ## PostProcess/Timelapse Fields
- ## P2 Niche Fields
- ## Straggler Fields (from partially-mapped sections)
- ## G-code Template Variables (upstream variable | our variable | status)
- ## Passthrough Promotion Candidates
- ## Summary Statistics

Each field row should indicate: upstream key name(s), our config field path, whether the typed field already exists or needs creation, whether mapping exists or needs creation.
  </action>
  <verify>
    <automated>test -f designDocs/PHASE34_FIELD_INVENTORY.md && grep -c "##" designDocs/PHASE34_FIELD_INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - designDocs/PHASE34_FIELD_INVENTORY.md exists
    - File contains "## Support Config Fields" section with at least 15 field rows
    - File contains "## Scarf Joint Fields" section
    - File contains "## Multi-Material Fields" section
    - File contains "## Custom G-code Hook Fields" section
    - File contains "## PostProcess" section
    - File contains "## P2 Niche Fields" section with at least 15 field rows
    - File contains "## G-code Template Variables" section
    - File contains "## Summary Statistics" with total counts
    - Each field row indicates upstream key name(s) and our config field path
  </acceptance_criteria>
  <done>Complete field inventory exists with all upstream keys categorized and cross-referenced against current codebase, ready for implementation</done>
</task>

</tasks>

<verification>
- designDocs/PHASE34_FIELD_INVENTORY.md exists and has all required sections
- Field counts are reasonable (>60 total fields across all sections)
</verification>

<success_criteria>
Definitive field inventory produced from real profile scanning, not just documentation. Every field that Plans 02-06 will implement is listed with its upstream key(s) and target config path.
</success_criteria>

<output>
After completion, create `.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-01-SUMMARY.md`
</output>
