---
phase: 34-support-config-and-advanced-feature-profile-import-mapping
plan: 03
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - crates/slicecore-engine/src/profile_import.rs
  - crates/slicecore-engine/src/profile_import_ini.rs
autonomous: true
requirements: [SCARF-MAP, MULTI-MAP, GCODE-MAP]

must_haves:
  truths:
    - "OrcaSlicer seam_slope_* fields import into typed ScarfJointConfig"
    - "OrcaSlicer wipe_tower_* and prime_* fields import into typed MultiMaterialConfig"
    - "OrcaSlicer G-code hook fields (start_gcode, end_gcode, etc.) import into typed CustomGcodeHooks"
    - "PrusaSlicer equivalents for these sections are also mapped"
  artifacts:
    - path: "crates/slicecore-engine/src/profile_import.rs"
      provides: "ScarfJoint, MultiMaterial, CustomGcode JSON mappings"
      contains: "seam_slope_type"
    - path: "crates/slicecore-engine/src/profile_import_ini.rs"
      provides: "PrusaSlicer INI mappings for these sections"
      contains: "wipe_tower"
  key_links:
    - from: "crates/slicecore-engine/src/profile_import.rs"
      to: "crates/slicecore-engine/src/config.rs"
      via: "config.scarf_joint.* and config.multi_material.* assignments"
      pattern: "config\\.(scarf_joint|multi_material|custom_gcode)\\."
---

<objective>
Map ScarfJointConfig (13 fields), MultiMaterialConfig (7 fields), and CustomGcodeHooks (5 fields) from upstream profiles into typed fields.

Purpose: These three sections are at 0% upstream mapping. Together they represent ~25 fields of unmapped config.
Output: Complete field mappings in both profile_import.rs and profile_import_ini.rs for all three sections.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-CONTEXT.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-RESEARCH.md
@designDocs/PHASE34_FIELD_INVENTORY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ScarfJoint and MultiMaterial JSON+INI field mappings</name>
  <files>crates/slicecore-engine/src/profile_import.rs, crates/slicecore-engine/src/profile_import_ini.rs</files>
  <read_first>
    - crates/slicecore-engine/src/profile_import.rs
    - crates/slicecore-engine/src/profile_import_ini.rs
    - crates/slicecore-engine/src/config.rs
    - designDocs/PHASE34_FIELD_INVENTORY.md
  </read_first>
  <action>
**ScarfJoint OrcaSlicer JSON mappings** (add to apply_field_mapping):
- `"seam_slope_type"` -> `config.scarf_joint.enabled = value != "none" && value != "0"` (bool derived)
- `"seam_slope_conditional"` -> `config.scarf_joint.conditional_scarf` (bool)
- `"seam_slope_start_height"` -> `config.scarf_joint.scarf_start_height` (f64)
- `"seam_slope_entire_loop"` -> `config.scarf_joint.scarf_around_entire_wall` (bool)
- `"seam_slope_min_length"` -> `config.scarf_joint.scarf_length` (f64)
- `"seam_slope_steps"` -> `config.scarf_joint.scarf_steps` (u32)
- `"seam_slope_inner_walls"` -> `config.scarf_joint.scarf_inner_walls` (bool)
- `"seam_slope_speed"` -> `config.scarf_joint.scarf_speed` (f64, if field exists -- check config.rs)
- `"wipe_on_loops"` -> `config.scarf_joint.wipe_on_loop` (bool)
- Any additional seam_slope_* fields found in inventory

**ScarfJoint PrusaSlicer INI mappings** (add to apply_prusaslicer_field_mapping):
- PrusaSlicer does NOT have native scarf joint support, so only add mappings if the inventory shows PrusaSlicer equivalents. If none, add a comment: `// PrusaSlicer has no scarf joint equivalent -- skip`

**MultiMaterial OrcaSlicer JSON mappings**:
- `"wipe_tower"` | `"enable_prime_tower"` -> `config.multi_material.enabled` (bool, check if this is the right field)
- `"wipe_tower_x"` -> `config.multi_material.wipe_tower_x` (f64)
- `"wipe_tower_y"` -> `config.multi_material.wipe_tower_y` (f64)
- `"wipe_tower_width"` -> `config.multi_material.wipe_tower_width` (f64)
- `"prime_tower_width"` -> also map to wipe_tower_width (BambuStudio name)
- `"wipe_tower_rotation_angle"` -> `config.multi_material.wipe_tower_rotation_angle` (f64, may need new field)
- `"prime_volume"` | `"purge_volume"` -> `config.multi_material.purge_volume` (f64, check field name)
- `"single_extruder_multi_material"` -> `config.multi_material.single_extruder_mmu` (bool, may need new field)
- Any additional wipe_tower_* or prime_* fields from inventory

**MultiMaterial PrusaSlicer INI mappings**:
- `"wipe_tower"` -> `config.multi_material.enabled` (bool)
- `"wipe_tower_x"` -> `config.multi_material.wipe_tower_x` (f64)
- `"wipe_tower_y"` -> `config.multi_material.wipe_tower_y` (f64)
- `"wipe_tower_width"` -> `config.multi_material.wipe_tower_width` (f64)
- `"wipe_tower_rotation_angle"` -> if field was added
- `"single_extruder_multi_material"` -> if field was added

**If new fields are needed in config.rs or support/config.rs**, add them with:
- `#[serde(default)]` for backward compat
- Full doc comment with units and description
- Sensible defaults (OrcaSlicer baseline per user decision)

Add upstream_key_to_config_field and prusaslicer_key_to_config_field entries for all new keys.
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo check -p slicecore-engine 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - profile_import.rs contains match arm for "seam_slope_type"
    - profile_import.rs contains match arm for "seam_slope_conditional"
    - profile_import.rs contains match arm for "seam_slope_start_height"
    - profile_import.rs contains match arm for "seam_slope_steps"
    - profile_import.rs contains match arm for "wipe_tower_x"
    - profile_import.rs contains match arm for "wipe_tower_width"
    - profile_import_ini.rs contains match arm for "wipe_tower" (PrusaSlicer multi-material)
    - cargo check -p slicecore-engine succeeds
  </acceptance_criteria>
  <done>ScarfJoint (13 fields) and MultiMaterial (7+ fields) fully mapped from both OrcaSlicer JSON and PrusaSlicer INI</done>
</task>

<task type="auto">
  <name>Task 2: Add CustomGcodeHooks JSON+INI field mappings</name>
  <files>crates/slicecore-engine/src/profile_import.rs, crates/slicecore-engine/src/profile_import_ini.rs</files>
  <read_first>
    - crates/slicecore-engine/src/profile_import.rs
    - crates/slicecore-engine/src/profile_import_ini.rs
    - crates/slicecore-engine/src/custom_gcode.rs
    - designDocs/PHASE34_FIELD_INVENTORY.md
  </read_first>
  <action>
**CustomGcodeHooks OrcaSlicer JSON mappings** (in apply_field_mapping):
- `"before_layer_change_gcode"` | `"before_layer_gcode"` -> `config.custom_gcode.before_layer_change` (String)
- `"layer_change_gcode"` | `"layer_gcode"` | `"after_layer_change_gcode"` -> `config.custom_gcode.after_layer_change` (String)
- `"toolchange_gcode"` | `"tool_change_gcode"` -> `config.custom_gcode.tool_change` (String)
- `"color_change_gcode"` -> `config.custom_gcode.color_change` (String, if field exists)
- `"pause_print_gcode"` -> `config.custom_gcode.pause_print` (String, if field exists)

Note: `start_gcode` and `end_gcode` are likely already in MachineConfig, NOT CustomGcodeHooks. Check the inventory and map them wherever they belong (probably `config.machine.start_gcode` and `config.machine.end_gcode`). If they are already mapped, skip.

**CustomGcodeHooks PrusaSlicer INI mappings**:
- `"before_layer_gcode"` -> `config.custom_gcode.before_layer_change` (String)
- `"layer_gcode"` -> `config.custom_gcode.after_layer_change` (String)
- `"toolchange_gcode"` -> `config.custom_gcode.tool_change` (String)
- `"color_change_gcode"` -> `config.custom_gcode.color_change` (if exists)
- `"pause_print_gcode"` -> `config.custom_gcode.pause_print` (if exists)
- `"start_gcode"`, `"end_gcode"` -> check if already mapped to machine config, if not add to correct location

For G-code hook string fields, use `value.to_string()` assignment (not bool/numeric parsing).

Add upstream_key_to_config_field and prusaslicer_key_to_config_field entries for all new keys.

Verify: `cargo test -p slicecore-engine --lib`
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - profile_import.rs contains match arm for "before_layer_change_gcode" or "before_layer_gcode"
    - profile_import.rs contains match arm for "toolchange_gcode" or "tool_change_gcode"
    - profile_import_ini.rs contains match arm for "before_layer_gcode"
    - profile_import_ini.rs contains match arm for "toolchange_gcode"
    - cargo test -p slicecore-engine --lib passes
  </acceptance_criteria>
  <done>CustomGcodeHooks (5 fields) fully mapped from both OrcaSlicer JSON and PrusaSlicer INI, all existing tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-engine --lib` passes
- All three sections (ScarfJoint, MultiMaterial, CustomGcode) have match arms in both importers
- `grep -c "config.scarf_joint\.\|config.multi_material\.\|config.custom_gcode\." crates/slicecore-engine/src/profile_import.rs` shows ~20+ matches
</verification>

<success_criteria>
ScarfJointConfig, MultiMaterialConfig, and CustomGcodeHooks all mapped from upstream profiles. Both JSON and INI importers updated. New fields added to config structs where needed.
</success_criteria>

<output>
After completion, create `.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-03-SUMMARY.md`
</output>
