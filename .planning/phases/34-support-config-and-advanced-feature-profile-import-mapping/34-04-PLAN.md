---
phase: 34-support-config-and-advanced-feature-profile-import-mapping
plan: 04
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - crates/slicecore-engine/src/config.rs
  - crates/slicecore-engine/src/profile_import.rs
  - crates/slicecore-engine/src/profile_import_ini.rs
autonomous: true
requirements: [POST-MAP, P2-FIELDS]

must_haves:
  truths:
    - "PostProcessConfig fields import from both OrcaSlicer and PrusaSlicer"
    - "All ~20 P2 niche fields have typed representation in config.rs"
    - "P2 fields import from upstream profiles where equivalents exist"
    - "Straggler fields from partially-mapped sections are now mapped"
  artifacts:
    - path: "crates/slicecore-engine/src/config.rs"
      provides: "New P2 typed fields and PostProcess/Timelapse mappable fields"
      contains: "timelapse_type"
    - path: "crates/slicecore-engine/src/profile_import.rs"
      provides: "P2 and PostProcess JSON mappings"
      contains: "slicing_tolerance"
  key_links:
    - from: "crates/slicecore-engine/src/profile_import.rs"
      to: "crates/slicecore-engine/src/config.rs"
      via: "config.post_process.* and new P2 field assignments"
      pattern: "config\\.(post_process|timelapse)"
---

<objective>
Map PostProcessConfig+subs (12 fields), add and map all ~20 P2 niche fields, and pick up straggler fields from partially-mapped sections.

Purpose: Achieve 100% typed field coverage for all upstream equivalents. PostProcessConfig is the last 0%-mapped section. P2 fields complete the niche coverage.
Output: New typed fields in config.rs, mappings in both importers, straggler fields closed.
</objective>

<execution_context>
@/home/steve/libslic3r-rs/.claude/get-shit-done/workflows/execute-plan.md
@/home/steve/libslic3r-rs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-CONTEXT.md
@.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-RESEARCH.md
@designDocs/PHASE34_FIELD_INVENTORY.md
@designDocs/CONFIG_PARITY_AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add P2 niche fields to config.rs and map PostProcess fields</name>
  <files>crates/slicecore-engine/src/config.rs, crates/slicecore-engine/src/profile_import.rs, crates/slicecore-engine/src/profile_import_ini.rs</files>
  <read_first>
    - crates/slicecore-engine/src/config.rs
    - crates/slicecore-engine/src/profile_import.rs
    - crates/slicecore-engine/src/profile_import_ini.rs
    - designDocs/PHASE34_FIELD_INVENTORY.md
    - designDocs/CONFIG_PARITY_AUDIT.md
  </read_first>
  <action>
**1. Add P2 niche fields to config.rs** (in appropriate sub-structs, all with `#[serde(default)]` and doc comments):

In `PrintConfig` (top-level) or appropriate sub-struct:
- `pub slicing_tolerance: SlicingTolerance` — new enum: `Middle`, `Nearest`, `Gauss` (default: Middle)
- `pub thumbnails: Vec<String>` — thumbnail size specs like "96x96", "400x300" (default: empty)
- `pub external_perimeter_extrusion_role: String` — role assignment for outer wall (default: empty string)

In `MachineConfig`:
- `pub silent_mode: bool` — enable silent/stealth mode (default: false)
- `pub nozzle_hrc: u32` — nozzle hardness rating HRC (default: 0)
- `pub emit_machine_limits_to_gcode: bool` — write M201/M203/M204 to G-code (default: false)
- `pub bed_custom_texture: String` — custom bed texture path (default: empty)
- `pub bed_custom_model: String` — custom bed 3D model path (default: empty)
- `pub extruder_offset: Vec<[f64; 2]>` — XY offset per extruder (default: empty)
- `pub cooling_tube_length: f64` — Bambu AMS tube length mm (default: 0.0)
- `pub cooling_tube_retraction: f64` — Bambu AMS tube retraction mm (default: 0.0)
- `pub parking_pos_retraction: f64` — AMS parking retraction mm (default: 0.0)
- `pub extra_loading_move: f64` — AMS extra loading distance mm (default: 0.0)

In `PrintConfig` or a new `MetaConfig` sub-struct:
- `pub compatible_printers_condition: Vec<String>` — cumulative condition expressions (default: empty)
- `pub inherits_group: String` — profile inheritance grouping (default: empty)
- `pub max_travel_detour_length: f64` — max detour for travel optimization mm (default: 0.0)

Add `SlicingTolerance` enum with `#[derive(Clone, Copy, Debug, Default, PartialEq, Serialize, Deserialize)]` and serde rename for lowercase.

**2. Map PostProcessConfig fields** in both importers:
- `"post_process"` -> `config.post_process.scripts` as `Vec<String>` (split by semicolon or newline)
- `"timelapse_type"` -> `config.post_process.timelapse.timelapse_type` or equivalent field (check config.rs for TimelapseConfig structure)
- Map any other PostProcess/Timelapse fields found in inventory

**3. Map all new P2 fields** in both importers:
- JSON: `"slicing_tolerance"` -> SlicingTolerance enum mapping
- JSON: `"thumbnails"` -> parse as comma-separated or JSON array
- JSON: `"silent_mode"` -> bool
- JSON: `"nozzle_hrc"` -> u32
- JSON: `"emit_machine_limits_to_gcode"` -> bool
- JSON: `"bed_custom_texture"` -> String
- JSON: `"bed_custom_model"` -> String
- JSON: `"extruder_offset"` -> parse array
- JSON: `"cooling_tube_length"` / `"cooling_tube_retraction"` / `"parking_pos_retraction"` / `"extra_loading_move"` -> f64
- JSON: `"compatible_printers_condition_cummulative"` -> Vec<String>
- JSON: `"inherits_group"` -> String
- JSON: `"max_travel_detour_length"` -> f64
- INI equivalents where they exist (PrusaSlicer has post_process, silent_mode, etc.)

**4. Pick up straggler fields** from inventory:
- IroningConfig: map `ironing_angle` if unmapped
- SequentialConfig: map any unmapped fields per inventory
- Any other partially-mapped section stragglers

Add upstream_key_to_config_field entries for all new keys.
  </action>
  <verify>
    <automated>cd /home/steve/libslic3r-rs && cargo test -p slicecore-engine --lib 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - config.rs contains `pub slicing_tolerance` field
    - config.rs contains `pub thumbnails` field
    - config.rs contains `pub silent_mode` field
    - config.rs contains `pub nozzle_hrc` field
    - config.rs contains `SlicingTolerance` enum
    - profile_import.rs contains match arm for "slicing_tolerance"
    - profile_import.rs contains match arm for "timelapse_type"
    - profile_import.rs contains match arm for "thumbnails"
    - profile_import.rs contains match arm for "post_process"
    - profile_import_ini.rs contains match arm for "post_process"
    - cargo test -p slicecore-engine --lib passes
  </acceptance_criteria>
  <done>All P2 niche fields added as typed config fields, PostProcessConfig mapped, straggler fields closed, all existing tests pass</done>
</task>

</tasks>

<verification>
- `cargo test -p slicecore-engine --lib` passes
- All 20 P2 fields from CONFIG_PARITY_AUDIT.md have typed representation
- PostProcessConfig has upstream mapping in both importers
</verification>

<success_criteria>
PostProcessConfig mapped from upstream. All ~20 P2 niche fields have typed config fields with upstream mappings. Straggler fields from partially-mapped sections are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/34-support-config-and-advanced-feature-profile-import-mapping/34-04-SUMMARY.md`
</output>
