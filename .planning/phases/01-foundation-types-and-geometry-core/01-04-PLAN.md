---
phase: 01-foundation-types-and-geometry-core
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - .github/workflows/ci.yml
  - .gitignore
  - .rustfmt.toml
  - clippy.toml
autonomous: true

must_haves:
  truths:
    - "cargo build --target wasm32-unknown-unknown succeeds for all three Phase 1 crates"
    - "cargo test runs all tests across all three crates and passes"
    - "cargo clippy produces zero warnings across all crates"
    - "CI configuration exists that validates WASM compilation, tests, clippy, and rustfmt on every push"
    - "Cross-crate integration works: slicecore-geo uses slicecore-math types, slicecore-mesh uses slicecore-math types"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline with WASM build, tests, clippy, rustfmt checks"
      contains: "wasm32-unknown-unknown"
    - path: ".gitignore"
      provides: "Standard Rust gitignore"
      contains: "target/"
    - path: ".rustfmt.toml"
      provides: "Rust formatter configuration"
      contains: "edition"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "all crates"
      via: "cargo build/test/clippy commands"
      pattern: "cargo.*wasm32-unknown-unknown"
    - from: "crates/slicecore-geo"
      to: "crates/slicecore-math"
      via: "Cargo dependency + type imports"
      pattern: "slicecore-math"
    - from: "crates/slicecore-mesh"
      to: "crates/slicecore-math"
      via: "Cargo dependency + type imports"
      pattern: "slicecore-math"
---

<objective>
Validate WASM compilation for all Phase 1 crates, set up CI configuration, and verify cross-crate integration. This plan is the final gate for Phase 1 -- proving that the foundation compiles to WebAssembly and all success criteria are met.

Purpose: Phase 1 success criterion #5 requires `cargo build --target wasm32-unknown-unknown` to succeed with zero errors. This plan installs the WASM target, validates compilation, fixes any issues, and creates CI configuration to enforce this going forward. It also validates the full test suite across all crates and sets up project-level tooling (gitignore, rustfmt, clippy config).

Output: WASM-validated codebase with CI configuration. Phase 1 complete.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-types-and-geometry-core/01-RESEARCH.md
@.planning/phases/01-foundation-types-and-geometry-core/01-01-SUMMARY.md
@.planning/phases/01-foundation-types-and-geometry-core/01-02-SUMMARY.md
@.planning/phases/01-foundation-types-and-geometry-core/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WASM compilation validation and issue resolution</name>
  <files>
    .gitignore
    .rustfmt.toml
    clippy.toml
  </files>
  <action>
    1. Set up Rust toolchain if needed:
       - Run `rustup default stable` if no default toolchain is set
       - Run `rustup target add wasm32-unknown-unknown` to install the WASM target
       - Verify: `rustup target list --installed` shows wasm32-unknown-unknown

    2. Run the WASM compilation test:
       - `cargo build --target wasm32-unknown-unknown`
       - If it fails, diagnose and fix the issue. Common problems:
         a. Dependencies using std::fs, std::net, std::thread -- check with `cargo tree` for offending deps
         b. clipper2-rust (or alternative polygon library) may not compile to WASM -- if so, gate it behind a feature flag or find a WASM-compatible alternative
         c. OnceLock (used in TriangleMesh for lazy BVH) should work on WASM -- verify
       - Fix any issues by: feature-gating problematic code, replacing incompatible deps, or adjusting code

    3. Run full test suite across all crates:
       - `cargo test --workspace`
       - Verify all tests pass. If any fail, fix them.
       - Count total tests: should be 80+ across all three crates

    4. Run linting:
       - `cargo clippy --workspace -- -D warnings` -- fix any warnings
       - `cargo fmt --all -- --check` -- fix any formatting issues

    5. Create `.gitignore` (standard Rust project):
       ```
       /target/
       **/*.rs.bk
       *.pdb
       .DS_Store
       ```

    6. Create `.rustfmt.toml`:
       ```toml
       edition = "2021"
       max_width = 100
       use_small_heuristics = "Default"
       ```

    7. Create `clippy.toml` (optional, only if needed for specific lints):
       ```toml
       # Clippy configuration for libslic3r-rs
       too-many-arguments-threshold = 8
       ```

    8. Run `cargo fmt --all` to format all code consistently.

    9. Run final verification:
       - `cargo build --workspace` -- native build succeeds
       - `cargo build --target wasm32-unknown-unknown` -- WASM build succeeds
       - `cargo test --workspace` -- all tests pass
       - `cargo clippy --workspace -- -D warnings` -- zero warnings
       - `cargo doc --workspace --no-deps` -- docs build cleanly
  </action>
  <verify>
    `cargo build --target wasm32-unknown-unknown` exits with code 0.
    `cargo test --workspace` exits with code 0 with all tests passing.
    `cargo clippy --workspace -- -D warnings` exits with code 0.
    `cargo fmt --all -- --check` exits with code 0 (all formatted).
  </verify>
  <done>
    All three Phase 1 crates compile to wasm32-unknown-unknown. Full test suite passes. Clippy clean. Code formatted. Project-level config files in place.
  </done>
</task>

<task type="auto">
  <name>Task 2: CI configuration and phase completion verification</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
    1. Create `.github/workflows/ci.yml`:
       ```yaml
       name: CI

       on:
         push:
           branches: [main, master, "phase-*"]
         pull_request:
           branches: [main, master]

       env:
         CARGO_TERM_COLOR: always
         RUSTFLAGS: "-D warnings"

       jobs:
         check:
           name: Check
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - run: cargo check --workspace

         test:
           name: Test
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
             - uses: Swatinem/rust-cache@v2
             - run: cargo test --workspace

         clippy:
           name: Clippy
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
               with:
                 components: clippy
             - uses: Swatinem/rust-cache@v2
             - run: cargo clippy --workspace -- -D warnings

         fmt:
           name: Format
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
               with:
                 components: rustfmt
             - run: cargo fmt --all -- --check

         wasm:
           name: WASM Build
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: dtolnay/rust-toolchain@stable
               with:
                 targets: wasm32-unknown-unknown
             - uses: Swatinem/rust-cache@v2
             - run: cargo build --target wasm32-unknown-unknown
       ```

    2. Verify all Phase 1 success criteria are met:

       **Criterion 1:** "Integer coordinate types (Coord/IPoint2) exist with documented precision strategy, and converting between float mesh coordinates and integer polygon coordinates round-trips without losing meaningful precision"
       - Verify: Coord is i64, COORD_SCALE is 1_000_000, round-trip tests pass in slicecore-math

       **Criterion 2:** "Polygon boolean operations (union, intersection, difference, XOR) produce correct results on a suite of 20+ test cases including degenerate geometry"
       - Verify: Count boolean tests in slicecore-geo: `cargo test -p slicecore-geo -- --list 2>&1 | grep -c boolean` shows 20+

       **Criterion 3:** "Polygon offsetting (inward and outward) produces correct results, validated against Clipper2 reference output"
       - Verify: Offset tests pass in slicecore-geo

       **Criterion 4:** "TriangleMesh data structure exists with BVH-accelerated spatial queries, uses arena+index pattern (no Rc/RefCell), and is Send+Sync"
       - Verify: Send+Sync compile test passes, no Rc/RefCell in crate: `grep -r "Rc\|RefCell" crates/slicecore-mesh/src/` returns nothing

       **Criterion 5:** "cargo build --target wasm32-unknown-unknown succeeds with zero errors on all Phase 1 crates"
       - Verify: WASM build succeeded in Task 1

    3. Print a summary of test counts per crate:
       - `cargo test -p slicecore-math -- --list 2>&1 | tail -1`
       - `cargo test -p slicecore-geo -- --list 2>&1 | tail -1`
       - `cargo test -p slicecore-mesh -- --list 2>&1 | tail -1`
  </action>
  <verify>
    `.github/workflows/ci.yml` exists and is valid YAML.
    All 5 Phase 1 success criteria verified (see checks in action step 2).
    `cargo build --target wasm32-unknown-unknown` succeeds.
    `cargo test --workspace` passes.
    No Rc/RefCell in slicecore-mesh source.
  </verify>
  <done>
    CI pipeline configured with 5 jobs: check, test, clippy, fmt, WASM build. All Phase 1 success criteria verified. The foundation is locked in: coordinate types, polygon operations, and mesh data structures are ready for downstream algorithm crates.
  </done>
</task>

</tasks>

<verification>
- `cargo build --target wasm32-unknown-unknown` -- succeeds with zero errors
- `cargo test --workspace` -- all tests pass (80+ total)
- `cargo clippy --workspace -- -D warnings` -- zero warnings
- `cargo fmt --all -- --check` -- all formatted
- `.github/workflows/ci.yml` exists with WASM build job
- No Rc/RefCell in slicecore-mesh: `grep -r "Rc\|RefCell" crates/slicecore-mesh/src/` returns empty
- Phase 1 success criteria 1-5 all verified
</verification>

<success_criteria>
- WASM compilation passes for all Phase 1 crates
- CI configuration enforces WASM build, tests, clippy, fmt on every push
- All Phase 1 success criteria from ROADMAP.md are met
- 80+ tests pass across the workspace
- Codebase is clean, formatted, and documented
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-types-and-geometry-core/01-04-SUMMARY.md`
</output>
